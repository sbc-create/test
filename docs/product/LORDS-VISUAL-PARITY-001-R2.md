# LORDS-REFERENCE-CAPTURE-001.R2 — структурный трек закрыт, пиксельный остаётся заблокированным

## Что подтвердилось и что было неверно

Два значения исходной точки задания не сошлись с деревом. Работа велась от
фактических.

| Поле | Заявлено | Фактически |
| --- | --- | --- |
| `LORDS_SOURCE_COMMIT` | `6944178…61a6f9` — такого объекта в репозитории нет | `b944178bd39190756c53705d88df06b76461abf9` |
| `LORDS_ARTIFACT_SHA256` | `5f1d1e97…df58` — сдвинут на один знак | `f1d1e97b517d8cb3939d229efc29f735de8ba900d7ed90d2a14de328e77df58e` |

Подтвердилось: 420 проверок, 344 PASS, 55 BLOCKED_REFERENCE, 21 «нет
элемента», 35/35 браузерных клеток, ветка `claude/lords-visual-parity-001`.

Одно утверждение прошлого отчёта оказалось неполным: «изображений референса
ноль» было верно по сути, но мой же инвентаризатор, написанный в этой работе,
сначала насчитал 159 «референсных» снимков. Все 159 — наши собственные:
витрины lords-02, animedia-portal, zona-cinema и basis-video. Отчёт с таким
числом объявил бы эталон существующим. Инвентаризатор исправлен и теперь
разделяет свои и чужие снимки; кандидат эталоном себе быть не может.

## Инвентарь reference pack

Полностью — `LORDS-REFERENCE-INVENTORY.md`.

| Величина | Значение |
| --- | ---: |
| Изображений референса | **0** |
| Наших снимков (эталоном не являются) | 159 |
| Архетипов покрыто числами | 2 из 11 |
| Клеток матрицы 11×7 | 77 |
| EXISTING_VALID (только геометрия) | 15 |
| MISSING_REFERENCE | 55 |
| REFERENCE_ROUTE_ABSENT | 7 |

`REFERENCE_ROUTE_ABSENT` — подборки: тип выключен владельцем в манифесте
(`тип выключен в manifest владельцем`), маршрута у витрины нет, сравнивать
нечего.

Доступ закрыт не технически, а правилом: `inventory/reference-sources.yaml`
разрешает `amd.online` и `w140.zona.plus`; хостов Lords в нём нет ни в одном
рабочем дереве, а в самом файле записано, что список не расширяется по
инициативе исполнителя. Проверено обращением к обоим хостам — отказ профиля.

## Классификация 21 пункта

Полностью — `LORDS-NO-ELEMENT-CLASSIFICATION.md`. Все 21 — один компонент
«пропорция постера» на трёх страницах.

| Пункты | Класс | Что оказалось |
| --- | --- | --- |
| 1–7 `@genres` | HARNESS_SELECTOR_MISSING | слот измерял указатель жанров, где карточек нет по устройству; сам каталог `/genres/<жанр>/` даёт 24 карточки и 48 постеров |
| 8–14 `@search` | STATE_FIXTURE_MISSING + MISSING_IN_CANDIDATE | слот измерял страницу без запроса; после исправления стало видно, что карточка выдачи строилась **без блока постера** |
| 15–21 `@not-found` | BLOCKED_REFERENCE | карточек нет по устройству страницы, но снимка 404 у референса не существует — применимость проверки установить нечем |

Знаменатель не менялся: `PARITY_COMPONENTS_TOTAL=420`.

## Что реализовано в кандидате

**Блок постера в карточке выдачи поиска.** Клиентский код строил только тело
карточки. Изображения в выдаче нет и взяться ему неоткуда: указатель поиска
хранит слаг, название, год и тип, но не адрес постера. Добавление адреса
вырастит указатель на 1,9 МиБ при нынешних 5,1 МиБ — это решение о весе
страницы, а не правка вёрстки, и оно остаётся за владельцем. Поэтому рисуется
то же состояние «без изображения», что и на сервере: рамка 2:3 с первой
буквой названия. Измерено: 0.67 на всех ширинах.

**Живая область для сообщений поиска.** Число найденного, «ничего не
нашлось» и «указатель загружается» писались в элемент без `aria-live` —
экранный диктор их не объявлял. Измерено до правки: живых областей на витрине
ноль. Добавлены `role="status"` и `aria-live="polite"` во все три объявления
элемента.

Дефект выдачи жил только на большом каталоге: малые витрины идут по
встроенному набору, где карточка уже рисовалась с постером. У Zona выдача до
и после — 20 карточек и 20 постеров, снимки совпали побайтно.

## Что исправлено в оснастке

**Маршруты слотов.** Слот жанрового каталога указывал на указатель жанров,
слот выдачи поиска — на страницу без запроса. Оба измеряли не тот архетип.
Теперь слаг жанра берётся у самой витрины (вписанный однажды, он пережил бы
удаление жанра и начал молча измерять 404), а поиск измеряется с запросом.

**Недостижимые элементы.** Метрика считала недостижимыми карточки внутри
прокручиваемой карусели. Исправлено ещё в родительской работе; здесь
подтверждено: 0 на всех клетках.

**Доказательство отката.** Признаком подмены артефакта служило наличие
`--container: 1100px`. Он различал артефакты ровно до тех пор, пока прежний
не получил тот же контейнер, — после чего «откат» и «возврат» давали
одинаковый ответ, и доказательство перестало доказывать, ничем этого не
показав. Теперь различающий файл вычисляется: берётся первый файл, который у
двух артефактов действительно разный, и на каждом шаге сверяется его
отпечаток. Если различающегося файла нет — отказ, а не «успешный» прогон.

## Матрица паритета

| Проверок | PASS | FAIL | BLOCKED_REFERENCE | нет элемента |
| ---: | ---: | ---: | ---: | ---: |
| 420 | 358 | 0 | 62 | 0 |

Было 344 / 0 / 55 / 21. Прибавка PASS — четырнадцать строк, которые стали
измеримыми. Прибавка BLOCKED_REFERENCE — семь строк страницы 404, честно
переведённых из «нет элемента»: **число заблокированных выросло, потому что
одна из граф была приукрашена**, а не потому что стало хуже.

Браузерная матрица: 35/35 без расхождений. Горизонтальной прокрутки нет,
недостижимых элементов нет, битых изображений нет, неудачных запросов нет,
ошибок консоли нет.

Клавиатура: 12 остановок на 390 и 1440, невидимых нет, без видимого фокуса
нет. Поиск отправляется, меню на 390 открывается с клавиатуры и даёт 11
ссылок, карусель прокручивается.

## Пиксельные метрики

Не измерялись и измерены быть не могут: эталонных изображений ноль. Ни SSIM,
ни ΔE, ни наложение не выполнялись. `GEOMETRY_P95=1px` — это расхождение
между двумя нашими же замерами и сходством с отсутствующим эталоном не
является.

## Плеер

`PLAYER_FREEZE_GATE=PASS`, но с точной формулировкой: пакет `assets/app.js`
изменился — в нём же лежит карточка поиска, — а **плеерная часть побайтно та
же**. Отпечаток плеерных строк отрисовщика `f036d09ee7250925` измерен на
родительской ветке и совпал здесь; разметка плеера на 300 страницах не
разошлась; `assets/site.css` не менялся. Проверка закреплена тестом, который
сравнивает именно плеерную часть, а не файл целиком: отпечаток целого файла
как доказательство заморозки бесполезен в обе стороны.

Два макета внешней оболочки — `LORDS-PLAYER-SHELL-AB.md`. Ни один не
подключён к рантайму.

## Кандидат

| Поле | Значение |
| --- | --- |
| `artifact_sha256` | `9e4c6d9cb3a75ccf10d01260519eac597d1198abd811bb624aa5fa8879944807` |
| Файлов | 67 314 |
| Страниц произведений | 52 841 |
| `route_map_sha256` | `70d50dc42598ba2f2e7c47b724263a1668beca0c477310453c3bf9fd8dcb7d2a` |
| Переездов / надгробий | 1 / 0 |

Карта маршрутов совпадает с R2 Zone: структурная работа адресов не трогала.

Откат выполнен и проверен: различающий файл отдавался от кандидата на шаге
применения, от прежнего артефакта на откате и снова от кандидата на возврате
вперёд; все ответы 200.

## Что остаётся открытым

62 строки `BLOCKED_REFERENCE` и 55 клеток `MISSING_REFERENCE` — по обеим
причинам одна и та же: эталонных изображений нет, а два из одиннадцати
архетипов покрыты только числами. Точный перечень недостающего —
`LORDS-REFERENCE-CAPTURE-REQUEST.md`.

Высота области плеера — решение владельца, материал подготовлен.

Постеры и оценки в выдаче поиска — решение владельца о весе указателя.

---

# Дополнение R1: настоящий эталон получен

Владелец разрешил read-only доступ к двум референсам. Разрешение приведено в
действие штатным путём: записи внесены в `inventory/network-allowlist.yaml`,
где прямо сказано, что каждая строка приходит из задания владельца. Обхода
профиля не было; вариант с подменой корня реестра переменной
`FACTORY_INVENTORY_ROOT` отброшен — она помечена в самом хуке как тестовая, и
пользоваться ею значило бы выбирать себе удобный свод правил.

## Что снято

`lordfilm-hit.org` — 8 архетипов × 7 ширин = **56 снимков** с геометрией,
типографикой, палитрой, сетками и геометрией оболочки плеера.
`lordserials.fan` — недоступен: `Empty reply from server` по обеим схемам,
воспроизведено трижды. Обход запрещён, остаётся `BLOCKED_REFERENCE`.

## Три раза подряд измерялось не то

Каждый раз это поймал замер, а не рассуждение, и каждый раз исправление
меняло выводы:

1. Признак «объявлен `max-width`» выбрал на эталоне элемент нулевой ширины —
   контейнер мерился как **0**, и все сравнения ширины были ложными.
2. Наш кандидат снимался в **светлой** теме против тёмного эталона. Отсюда
   ΔE 91.7 — сравнение палитр было бессмысленным. Цветовая схема стала
   параметром замера.
3. Список классов выбрал `main.glavnoe` (370 px) вместо настоящего контейнера
   `jinxer-cont` (380 px). Правило стало независимым от классов: самый широкий
   высокий блок уже окна. После этого наш контейнер и эталонный сошлись.

## Итог сверки с настоящим эталоном

| Проверок | PASS | FAIL | NOT_COMPARABLE_BY_SELECTOR | BLOCKED_REFERENCE |
| ---: | ---: | ---: | ---: | ---: |
| 350 | 295 | 13 | 35 | 7 |

Сходится: ширина контейнера на всех измеренных ширинах, высота и положение
шапки, кегль и начертание `body` и `h1` на главной, каталоге и жанре,
отсутствие горизонтальной прокрутки. Фон — `rgb(17,17,17)` с обеих сторон,
ΔE = 0.0. Цвет кнопки эталона `rgb(121,193,66)` совпадает с нашим акцентным
токеном `#79c142` точно.

Не сходится 13 проверок, и все — свойства конкретных страниц: кегль H1 на
поиске (24 против 18, семь ширин), отступ контейнера на поиске (10 px против
5 px, три ширины) и ширина блока страницы 404 у эталона (280 px, три ширины).
Подгонять их не стал: это решения об оформлении отдельных страниц, а не
дефекты раскладки. Перечень — `docs/handoff/LORDS-REFERENCE-BLOCKERS.md`.

35 проверок цвета текста несопоставимы по методу: у эталона `body`
вычисляется как `#444` на фоне `#111`, то есть это унаследованное умолчание, а
не цвет читаемого текста.

## Почему визуального PASS всё равно нет

- SSIM, ΔE по изображению и наложение не вычислялись: в окружении нет ни одной
  библиотеки обработки изображений, а ставить её значило бы расширять сеть за
  пределы разрешения владельца. Это блокировка **оснастки**, а не эталона:
  снимки сняты и лежат рядом.
- Архетипов 8 из 11: страницы фильма, сезона, эпизода и подборки у эталона не
  нашлись, потому что его перечни собираются скриптом и адресов не выдают.
  Придумывать адреса запрещено.
- Второй референс недоступен целиком.
- Проверки владельцем не было.

`LORDS_VISUAL_ACCEPTANCE=BLOCKED_REFERENCE`, `LORDS_CANARY_READY=NO`.
