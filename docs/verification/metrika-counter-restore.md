# Восстановление счётчика Метрики после переноса в ячейку

## Что было сломано

При миграции витрин в переносимые ячейки значение счётчика потерялось: оно
объявлялось только в drop-in монолитных служб, например
`/etc/systemd/system/lords-nova-01.service.d/metrika.conf`. Юниты ячеек таких
drop-in не наследуют, переменная `LORDS_METRIKA_COUNTER` оставалась пустой, и
рантайм не выводил разметку счётчика. Публичный HTTP 200 это не показывал.

Затронуто четыре перенесённых домена. Отсюда правило: **при переносе сверять не
только файлы кода и данных, но и переменные окружения действующего юнита.**

## Исправление своего сайта

`config/site.json` → `environment.LORDS_METRIKA_COUNTER = "112582938"`,
коммит `350de09`, CI 36262379375 (success), выпуск через очередь:
`status=ok outcome=activated stage=live_verified`.

## Публичная проверка после выпуска

    zonafilm.space/                  build-id=350de098600c-zona-01
        инициализаций ym(...,'init')  1  [112582938]
        скриптов mc.yandex.ru/metrika/tag  1
        noscript mc.yandex.ru/watch/       [112582938]
    zonafilm.space/title/imperiya-artura/   build-id=350de098600c-zona-01
        инициализаций 1  [112582938]        плеер присутствует

Ровно одна инициализация на страницу — дубликат счётчика не создан.
Содержимое не пострадало: на главной 64 ссылки-карточки и 48 оценок.

## Три витрины Lords: та же правка, выпущены и проверены

Идентификаторы уже существовали в `config/analytics.json`; новые не заводились.
В каждом репозитории добавлена одна строка в `config/site.json` -> `environment`.

| домен | коммит | CI | build-id на странице | ym init | noscript | ссылок-карточек |
|---|---|---|---|---|---|---|
| lordfilm47.space | 2f64baea7d6d | 36265142692 | 2f64baea7d6d-lords-01 | 1 x 112010269 | 112010269 | 71 |
| lordserial33.biz | e90f7274abbc | 36265144065 | e90f7274abbc-lords-02 | 1 x 112010274 | 112010274 | 74 |
| 1lordserials1.online | a698288ee8b1 | 36265145580 | a698288ee8b1-lords-03 | 1 x 112010277 | 112010277 | 190 |

Все три выпуска прошли через очередь: `status=ok outcome=activated`. Ровно одна
инициализация на страницу — дубликатов нет.

Проверка `run.py --check` у lordserial33-biz и 1lordserials1-online падает на
отсутствии `config/player.json`. Это состояние рабочей копии до правки и к ней
отношения не имеет: файл в git не хранится (в репозитории только
`player.json.example`), его подставляет выкладка. У lordfilm47-space локальная
копия есть, и там проверка проходит с кодом 0, печатая
`LORDS_METRIKA_COUNTER=112010269`.

## Поступление данных — НЕ ПОДТВЕРЖДЕНО

Разметка на странице доказывает только отправку. Приходят ли данные в кабинет,
я проверить не могу: доступа нет. Нужен OAuth-токен Метрики с правом чтения
статистики (файлом вне репозитория, путь — в конфигурации). Topvisor в проекте
не настроен вовсе: ни ключа, ни идентификатора проекта.
