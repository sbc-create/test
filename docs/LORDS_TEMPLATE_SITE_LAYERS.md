# Совместимость 1.1.0 с собственными слоями действующих сайтов

Прошлая редакция таблицы совместимости говорила «снять дублирующий слой». Это
неверный совет: слой сайта — не копия шаблона, а смесь трёх разных вещей, и
часть из них шаблон не умеет и уметь не должен. Здесь каждый модуль разобран
по поведению, и для каждого сказано, что с ним делать.

Правило разбора одно: **сравнивается ПОВЕДЕНИЕ, а не имена файлов.** Модуль
снимается только там, где ядро 1.1.0 делает то же самое или строже. Всё
остальное остаётся сайту.

---

## lordserial33.biz (lords-02), слой `src/lords02_*`

| Модуль | Строк | Что делает | 1.1.0 | Решение |
| --- | --- | --- | --- | --- |
| `lords02_taxonomy.py` | 386 | классификация разделов, склонения, серии «доступно из заявленных» | `lords_sections.py` — та же классификация, тот же порядок приоритета, те же словари; **пришла отсюда** | **снять**, но сверив: у сайта есть `config/classification-overrides.json` — файл поимённых исправлений источника. В ядре механизм есть (`LORDS_CLASSIFICATION_OVERRIDES`), СПИСОК записей принадлежит сайту и переносится как есть |
| `lords02_community.py` | ~1100 | обвязка HTTP сообщества | `community_http.py` — тот же контракт, вынесенный в шаблон; **пришла отсюда** | **снять**. Имена куки сохранить прежними (`ls_v`, `ls_mod`, `ls_draft`) и соль токена тоже: иначе у всех посетителей разом слетят и голос, и черновик. Шаблон выводит имена из `site_id`, для lords-02 это даст другие — нужен явный `Настройка(...)` с прежними значениями |
| `lords02_view.py` | 1484 | ОФОРМЛЕНИЕ витрины: 173 правила стилей, своя разметка главной, карточки, шапки | ядро даёт другое оформление (профиль `lords-new`) | **оставить**. Это лицо сайта, а не дефект. Шаблон менять его не должен |
| `lords02_collections.py` | 283 | восемь тематических подборок с обложками-коллажами | ядро даёт подборки контракта (6 ключей) с четырьмя постерами | **оставить до сравнения**: наборы разные по составу и по числу. Замена сменила бы содержание витрины, а не починила дефект |
| `lords02_handler.py` | 166 | свои маршруты витрины | ядро добавило `/anime/`, `/dorama/` | **сверить пересечение**: маршруты, которые теперь есть в ядре, снять; собственные оставить |
| `lords02_history.py` | 195 | журнал изменений состава блоков главной | в ядре нет | **оставить** |
| `lords02_reload.py` | 244 | перечитывание снимка по mtime без перезапуска | в ядре НЕТ намеренно (см. «Открытые зависимости») | **оставить**. Снять его значит ухудшить сайт: он потеряет подхват без перезапуска |

## 1lordserials1.online (lords-03), слой `overlay/*`

| Модуль | Строк | Что делает | 1.1.0 | Решение |
| --- | --- | --- | --- | --- |
| `shelves.py` | 348 | полки без дыр, отбор до предела, подборки с постерами | ядро делает то же (`РЯД_ПОЛКИ`, `lords_sections.набрать`, `_карточка_подборки`); **пришло отсюда** | **снять** после сверки чисел: у сайта `РЯД = 14`, `РЯД_СЛАЙДЕРА = 28`, `НА_СТРАНИЦЕ_ПОДБОРКИ = 84`; в ядре 14 и 28 совпадают, размер страницы подборки — 60. Либо принять 60, либо вынести в настройку витрины |
| `assets.py` | 539 | токены переменными, тёмная тема, шаг ленты, плотность сетки | ядро делает то же; **пришло отсюда** | **снять**, кроме: у сайта лента прячет хвост правилом `:nth-child(n+25)` — в ядре этого нет намеренно, и снятие слоя УБЕРЁТ сокрытие. Проверить, что владельца устраивает неполная последняя страница |
| `community.py` (overlay) | ~700 | обвязка HTTP сообщества | `community_http.py`; **пришла отсюда** | **снять** с сохранением имён куки (`l3_v`, `l3_mod`) и соли — как у lords-02 |
| `view.py` | 389 | разметка витрины: слайдер, блоки | ядро даёт свою (профиль `lords-curated`) | **оставить**: лицо сайта |
| `freshness.py` | 233 | перечитывание снимка по mtime | в ядре НЕТ намеренно | **оставить** |
| `newness.py` | 456 | разведение событий поступления: выход серии, озвучка, повторная доставка | в ядре НЕТ: требует отметки времени эпизода, которой в формате снимка нет | **оставить**. Ядро закрыло более узкий случай (окно новизны, пустая `new_episodes`), полное разведение — за производителем |
| `apply.py`, `core_loader.py`, `main.py` | ~250 | механика наложения на закреплённое ядро | в ядре не нужна, если слой снят целиком | **снять вместе с последним снятым модулем**, не раньше |

## Порядок, которым это делается

Не «снять слой», а по одному модулю, каждый со своей сверкой:

1. **Снимок поведения ДО.** Прогон приёмки сайта на его текущем выпуске:
   `checks/run.sh` плюс собственные проверки сайта (`lords02_surfaces`,
   `lords02_taxonomy`, `collections_distinct`, `block_fill`…). Это эталон.
2. **Один модуль.** Снять, продвинуть версию ядра, прогнать те же проверки.
3. **Сравнить.** Расхождение — это либо дефект переноса, либо особенность
   сайта, которую шаблон не умеет. Первое чинится в шаблоне, второе
   возвращается в слой и записывается сюда.
4. **Только после зелёного** — следующий модуль.

Один выпуск — один модуль. Смешивать нельзя: когда что-то сломается,
откатывать придётся всё сразу.

## Что обязано совпасть до снятия, иначе пострадают посетители

| Что | Почему |
| --- | --- |
| имена куки посетителя и модератора, соль токена | смена = у всех посетителей слетает голос, черновик и доступ модератора |
| путь хранилища сообщества | смена = витрина перестаёт видеть прежние голоса и комментарии |
| постоянный идентификатор темы (`details[slug]["id"]`) | смена ключа = все оценки теряют привязку к произведению |
| адреса существующих страниц | `/anime/` и `/dorama/` ДОБАВЛЯЮТСЯ; ни один прежний адрес не исчезает |
| режим индексации | у каждого сайта свой, шаблон его не задаёт |

## Чего шаблон не заменит никогда

Оформление (`lords02_view.py`, `overlay/assets.py` в части палитры и
разметки), собственные подборки, журнал изменений и правила свежести,
завязанные на договорённости конкретного сайта. Профиль задаёт лицо витрины, и
два сайта на одном ядре обязаны выглядеть по-разному — это записано правилом
исключительности в `config/template-pool.json`.
