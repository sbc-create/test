# Активация zonafilm.space — повторная попытка после отката

Первая попытка (коммит `25f6808a8388`) откатилась: новая служба не ответила за
40 секунд. Витрина не была сломана — она не успела. Прежняя служба после отката
поднималась 252 секунды, потому что читает снимок каталога 16.7 МБ и detail
78.3 МБ. Прежнюю команду повторять нельзя: в ней тот же предел.

## Шаг 1. Предварительная проверка (ничего не меняет)

```bash
cd /home/claude/wt-portable-site-cell-01 && \
sudo /home/claude/wt-portable-site-cell-01/.venv/bin/python -m factory cell activate \
  --site zona-01 \
  --commit 93a7106af2e0aed01cf98ecb9289487945914052 \
  --expect-digest sha256:562fe81b3c287085caf6946a5b573f39177c8a30f3178d1a429430337c89bdf4
```

`cd` обязателен: из другого каталога `python -m factory` подхватывает другую
копию пакета, в которой команды `cell` нет.

Проверка соберёт артефакт из коммита, сверит digest и выполнит
`deploy/activate.sh --dry-run` — то есть дойдёт до настоящих предусловий.
Ожидаемый вывод: `"status": "dry-run"`, `"exit_code": 0`.

## Шаг 2. Активация

```bash
cd /home/claude/wt-portable-site-cell-01 && \
sudo /home/claude/wt-portable-site-cell-01/.venv/bin/python -m factory cell activate \
  --site zona-01 \
  --commit 93a7106af2e0aed01cf98ecb9289487945914052 \
  --expect-digest sha256:562fe81b3c287085caf6946a5b573f39177c8a30f3178d1a429430337c89bdf4 \
  --confirm-activation
```

**Сайт будет недоступен около 4–5 минут.** Порт один (9120), поэтому прежняя
служба останавливается до запуска новой, а новая читает 95 МБ снимков. Это не
сбой, а стоимость переключения; скрипт печатает `загружается, N с из 600`.

Коды возврата различают исходы:

| Код | Что произошло |
| --- | --- |
| 0 | новая служба ответила на `/healthz`, переключение состоялось |
| 1 | новая не вышла на готовность, откат выполнен **и проверен ответом** прежней |
| 2 | отказ до мутаций (предусловия, digest, дерево репозитория) |
| 3 | откатились, но прежняя служба не отвечает — нужен человек немедленно |

## Что исправлено после первой попытки

**Ожидание готовности.** `SITE_HEALTH_TIMEOUT` по умолчанию 600 с (было 40),
`SITE_HEALTH_INTERVAL`, `SITE_HTTP_TIMEOUT`; каждый запрос ограничен
`--max-time`. Три различимых исхода вместо двух: отвечает / не успел при живом
процессе / процесс не работает. В последнем случае ожидание прекращается сразу
и печатается диагностика — `systemctl status` и последние 40 строк журнала.

**Откат тоже ждёт.** Возврат прежней службы проверяется тем же ожиданием.
`systemctl is-active` готовностью не считается: служба типа `simple` активна с
первой миллисекунды, задолго до первого ответа.

**Страницы прежнего релиза переехали в своё хранилище.** `LORDS_LEGACY_ROOT`
вёл на `/srv/lords/zona-01/current/site`. Это оказалось непреднамеренной
зависимостью: дерево не обновлялось 13 суток, `lords-content-refresh.sh` ведёт
только `lords-01..03`, а ссылка `current` принадлежит отключаемому процессу
выкладки — её исчезновение молча превратило бы страницы тайтлов в 404.
Активация копирует дерево (48 МБ, 1455 файлов) в `<data>/site`.

**Контракт юнита проверяется.** Прямо по жалобе из журнала:
`Environment=LORDS_SITE_NAME=Zona Cinema` без кавычек — systemd берёт `Zona`, а
`Cinema` объявляет `Invalid environment assignment`. Проверка прогнана на старом
юните и нашла ровно это. В новом юните строк `Environment=` нет вовсе:
окружение задаёт `run.py` из `config/site.json`, поэтому этот класс ошибки там
невозможен.

**Прежняя служба закрывается от ручного запуска, а не только отключается.**
`systemctl restart`
поднимает и отключённый юнит, а `automation/host/nova-daily-refresh.sh`
(таймер включён, 03:30 UTC, `Persistent=true`) после публикации выполняет
`systemctl restart lords-nova-01.service nova-zona-01.service || true`. На
следующие сутки после переноса прежняя служба попыталась бы занять порт 9120 под
новой — обычно ушла бы в failed незаметно, а в момент перезапуска новой заняла
бы порт и вернула прежний код. Закрывает это drop-in
`/etc/systemd/system/nova-zona-01.service.d/10-zonafilm-space-port-guard.conf`
с `[Unit] RefuseManualStart=yes`: `start` и `restart` отказывают явно, файл юнита
остаётся на месте. Откат удаляет drop-in первым действием — иначе `enable --now`
отказал бы и сайт остался бы без обеих служб.

`systemctl mask` для этого не годится и был бы опаснее болезни: он кладёт ссылку
на `/dev/null` по пути юнита, а `/etc/systemd/system/nova-zona-01.service`
существует как обычный файл — systemd отвечает «File … already exists». Под
`set -e` это оборвало бы активацию между «прежняя остановлена» и «новая
установлена». Способ подсказан сессией animedia.space, где mask отказал на живом
хосте.

**Остаётся для владельца:** строку `systemctl restart … nova-zona-01.service`
в `nova-daily-refresh.sh` нужно поправить — файл принадлежит ветке
`claude/players-only-production-009`, отсюда не правится. Иначе производитель
будет ежесуточно получать отказ на замаскированной службе.

## Состояние перед запуском

| Что | Факт |
| --- | --- |
| Коммит | `93a7106af2e0`, дерево чистое, отправлен |
| CI | success, run 35838438711, ровно на этом коммите |
| Сайт сейчас | публично 200, build-id `zona-01-a0209877e1db` |
| Прежняя служба | `nova-zona-01.service` включена и работает |
| Новая служба | отключена после отката, файл юнита на месте |
| Остатки прошлой попытки | `/srv/zonafilm-space/{app,app.prev,data}` — перезапишутся |
| Свой замок активации | снят (`.activate.lock` отсутствует) |
| Замок направления | отсутствует; архивированный не восстанавливаю |
| Проверки | 11 из 11 PASS, включая четыре выполненных сценария |

## После активации

```bash
cd /home/claude/wt-portable-site-cell-01
.venv/bin/python -m factory cell deliver --site zona-01
curl -s https://zonafilm.space/ | grep -o 'site-factory-build-id" content="[^"]*"'
curl -s -o /dev/null -w '%{http_code}\n' https://zonafilm.space/
```

Ожидаемый публичный build-id: `93a7106af2e0-zona-01`.
Базовый слепок для сравнения — `var/базовый-слепок.json`: главная 200 /
123 922 байта, `/catalog` и `/search` — 308, robots.txt закрыт,
`X-Robots-Tag: noindex, nofollow`.

Ручной откат: `sudo /home/claude/wt-portable-site-cell-01/var/site-repos/zonafilm-space/deploy/rollback.sh`
