# Матрица тестов управляющего слоя

Порождена из файлов набора, а не составлена по памяти: список, ведомый
отдельно от тестов, отстаёт при первом же добавлении.

Пересоздать: `python docs/generate_test_matrix.py` из корня репозитория.

| Область | Файл | Проверок |
|---|---|---|
| Control API: конвейер записи | `test_control_api.py` | 40 (+1 с наборами) |
| Транспорт HTTP | `test_api_http_transport.py` | 18 |
| Маршрутизация между слоями | `test_routing_guard.py` | 3 |
| Админка: приложение | `test_admin.py` | 31 |
| Админка: подача и заголовки | `test_admin_http.py` | 8 |
| Метрики | `test_metrics.py` | 15 |
| Контракт CMS | `test_compat.py` | 19 (+4 с наборами) |
| Изоляция и радиус поражения | `test_blast_radius.py` | 11 |
| Сверка описания с поведением | `test_site_engine_openapi.py` | 9 |
| Распределённые пределы и идемпотентность | `test_multiprocess_control.py` | 7 |
| Протокол запуска и жизненный цикл | `test_startup_lifecycle.py` | 20 |
| Сквозная трассировка | `test_tracing.py` | 21 |

**Всего в матрице: 202 проверок.**

## Что эти проверки специально не покрывают

- Отказ контейнера витрины и восстановление из резервной копии: обе операции
  затрагивают общий стенд, которым пользуются другие потоки.
- Нагрузочное поведение транспорта: пределы измерялись только на корректность отказа.
- Свежесть контента и полнота каталога: измеряются читающими маршрутами и
  функциональной проверкой каталога, а не счётчиками процесса.

## Уроки набора

Четыре дефекта нашли сквозные проверки, а не модульные, — и все четыре
проходили модульные тесты:

1. Подделанный CSRF с кириллицей давал 500 вместо 403: `compare_digest` со
   строками бросает `TypeError` на не-ASCII, а тест подставлял ASCII.
2. `/api/v1/metrics` отвечал 404: перечень управляющих маршрутов в транспорте
   вёлся литералом; сверка описания вызывает `ControlApi` напрямую, минуя транспорт.
3. Витрина с нечитаемым профилем управлялась как исправная: испорченный JSON
   превращался в пустой словарь, а тот означает «контракт не объявлен».
4. Объявление контракта в профилях сломало ворота качества: схема запрещает
   лишние свойства, а поле не было в ней объявлено.

Общее у всех четырёх: проверялось верное свойство, но на данных или на слое,
где дефект не проявляется.

## Control API: конвейер записи

Файл `tests/unit/test_control_api.py`.

- Test writes are disabled by default
- Test disabled writes do not leak existence
- Test reads stay available when writes disabled
- Test missing token is rejected
- Test unknown token is rejected
- Test scope is enforced per operation
- Test job scope does not grant config scope
- Test rate limit stops runaway automation
- Test rate limit names which limit was hit
- Test noisy site does not block its neighbour
- Test unknown site is 404
- Test malformed site id is rejected before lookup
- Test job action is a closed list
- Test dangerous settings are refused with a reason
- Test canonical host change is refused
- Test range is checked not only type
- Test bool is not accepted as int
- Test all problems are reported at once
- Test dry run reports diff without changing anything
- Test dry run job does not enqueue
- Test same key replays instead of repeating
- Test same key with different body is a conflict
- Test stale version is rejected
- Test current version is accepted and changes after write
- Test nested settings merge rather than replace
- Test no op change is reported as such
- Test targeted invalidation requires keys
- Test invalidation is queued not executed directly
- Test job status reflects queue stage
- Test mutation is recorded with diff
- Test refusal is recorded too
- Test raw token never reaches the audit log
- Test correlation id is returned even on failure
- Test supplied correlation id is preserved
- Test audit trail is readable and filterable
- Test закрытые на запись профили дают понятный отказ
- Test ключ кэша не может быть адресом
- Test обычные ключи кэша проходят
- Test недоступный файл счётчика не роняет обслуживание
- Test запасной режим строже обычного

## Транспорт HTTP

Файл `tests/unit/test_api_http_transport.py`.

- Test подача выключена по умолчанию
- Test читающий маршрут отвечает по http
- Test записывающий маршрут без токена отклонён
- Test записывающий маршрут с токеном проходит
- Test управляющее чтение принимает параметры строки запроса
- Test негодный limit в строке запроса не роняет сервер
- Test слишком большое тело отклоняется до разбора
- Test негодный json даёт 400 а не 500
- Test json не объект отклоняется
- Test запись в читающий маршрут запрещена
- Test удаление не предусмотрено
- Test ответы не кэшируются
- Test ответ об ошибке не раскрывает устройство
- Test публичная привязка требует явного разрешения
- Test локальная привязка разрешена без оговорок
- Test выключенная запись невидима и по http
- Test метрики отдаются текстом для сборщика
- Test метрики без токена не отдаются

## Маршрутизация между слоями

Файл `tests/unit/test_routing_guard.py`.

- Test каждый управляющий маршрут уходит в управляющий слой
- Test читающие маршруты не перехватываются
- Test перечень префиксов не ведётся руками

## Админка: приложение

Файл `tests/unit/test_admin.py`.

- Test без сессии страница показывает форму входа
- Test без сессии запись отвечает отказом а не формой
- Test неверный токен отклонён
- Test пустой и неверный токен неотличимы
- Test cookie защищена
- Test выход разрушает сессию
- Test сессия истекает по бездействию
- Test сессия истекает по общему сроку
- Test форма без csrf отклонена
- Test csrf не из ascii даёт отказ а не сбой
- Test csrf с суррогатом не роняет сравнение
- Test форма с чужим csrf отклонена
- Test список витрин показан
- Test карточка витрины показана
- Test проверка задания ничего не ставит
- Test боевая постановка создаёт задание
- Test недопустимое действие отклоняется апи
- Test опасная настройка отклонена с причиной
- Test настройка применяется со сверкой версии
- Test негодный json не доходит до апи
- Test инвалидация области title требует ключей
- Test панель не показывает недоступных действий
- Test но запрет держится апи а не разметкой
- Test токен не попадает в разметку
- Test значения экранируются
- Test журнал показывает и отказы
- Test неизвестная страница админки даёт 404
- Test панель показывает согласованный контракт
- Test панель показывает необъявленный контракт
- Test несовместимая витрина видна в списке
- Test действие над несовместимой витриной отклоняется

## Админка: подача и заголовки

Файл `tests/unit/test_admin_http.py`.

- Test без включённой админки маршрута нет
- Test страница входа отдаётся
- Test защитные заголовки на месте
- Test панель запрещает индексацию
- Test вход по http выдаёт cookie и перенаправляет
- Test неверный токен по http
- Test список витрин после входа
- Test слишком большая форма отклонена

## Метрики

Файл `tests/unit/test_metrics.py`.

- Test метрики требуют права чтения
- Test метрики без токена недоступны
- Test формат содержит help и type
- Test счётчик появляется только с первым наблюдением
- Test тип объявляется один раз на метрику
- Test значения меток экранируются
- Test запросы считаются по классу ответа
- Test отказы считаются по коду
- Test очередь видна по стадиям
- Test число витрин считается из профилей
- Test зарегистрированный показатель попадает в вывод
- Test упавший источник показателя не роняет опрос
- Test метки не содержат идентификаторов витрин
- Test класс ответа вместо точного кода
- Test счётчики обнуляются сбросом

## Контракт CMS

Файл `tests/unit/test_compat.py`.

- Test совпадающие контракты согласованы
- Test профиль без объявления считается совместимым
- Test младшая версия выше движковой это ограниченная работа
- Test младшая версия ниже допустима
- Test расхождение старших версий несовместимо
- Test неразобранная версия считается несовместимой
- Test испорченная версия движка не объявляет всё исправным
- Test несовместимой витриной управлять нельзя
- Test отказ называет обе версии
- Test ограниченной витриной управлять можно
- Test проверка совместимости не обходится пробным запуском
- Test матрица перечисляет витрины и состояния
- Test матрица по одной витрине
- Test матрица требует права чтения
- Test несуществующая витрина в матрице
- Test один движок обслуживает разные типы витрин
- Test схема знает о поле контракта
- Test схема проверяет формат версии
- Test все профили массива объявляют контракт

## Изоляция и радиус поражения

Файл `tests/unit/test_blast_radius.py`.

- Test испорченный профиль не задевает соседнюю
- Test пропавший профиль не задевает соседнюю
- Test чужой контракт не задевает соседнюю
- Test матрица показывает ровно одну пострадавшую
- Test занятая витрина не блокирует соседнюю
- Test блокировка освобождается после операции
- Test застрявшее задание возвращается в очередь
- Test трижды неудавшееся уходит в карантин
- Test недоступный журнал не превращает отказ в сбой
- Test метрики отдаются при недоступной очереди
- Test авария одной витрины видна в журнале поимённо

## Сверка описания с поведением

Файл `tests/unit/test_site_engine_openapi.py`.

- Test версия openapi
- Test каждый описанный маршрут принадлежит слою
- Test все описанные читающие маршруты отвечают
- Test все описанные управляющие маршруты отвечают
- Test каждый маршрут описывает ошибку
- Test страничные маршруты описывают пределы
- Test файл описания совпадает с порождённым
- Test ошибка всегда одной формы
- Test краткая карточка соответствует схеме

## Распределённые пределы и идемпотентность

Файл `tests/unit/test_multiprocess_control.py`.

- Test два процесса не создают второго задания
- Test общий ключ с другим телом даёт конфликт между процессами
- Test предел общий для процессов
- Test состояние предела переживает перезапуск
- Test идемпотентность переживает перезапуск
- Test отказ не занимает ключ навсегда
- Test недопустимый ключ даёт отказ а не сбой

## Протокол запуска и жизненный цикл

Файл `tests/unit/test_startup_lifecycle.py`.

- Test без профилей запуск невозможен
- Test испорченный профиль ограничивает а не валит
- Test столкновение доменов фатально
- Test разные домены проходят
- Test несовместимый контракт ограничивает
- Test чужая версия схемы профиля ограничивает
- Test запись без токенов фатальна
- Test значения токенов не попадают в отчёт
- Test негодный перечень токенов фатален
- Test недоступный каталог состояния фатален
- Test отчёт читается человеком
- Test слив перестаёт принимать запросы
- Test слив дожидается начатых
- Test слив не ждёт вечно
- Test счётчик начатых не уходит в минус
- Test без systemd уведомления безвредны
- Test период сторожа вдвое меньше назначенного
- Test без назначенного срока сторож не работает
- Test протокол запуска совпадает у службы и выкладки
- Test закрытые на запись профили ограничивают а не валят

## Сквозная трассировка

Файл `tests/unit/test_tracing.py`.

- Test негодный traceparent не отвергает запрос
- Test нулевые идентификаторы не принимаются
- Test контекст продолжается а не начинается заново
- Test шаблон пути вместо идентификаторов
- Test успех восстанавливается по следу
- Test отказ по праву восстанавливается
- Test конфликт версии восстанавливается
- Test повтор мутации восстанавливается
- Test занятая витрина восстанавливается
- Test недоступная витрина восстанавливается
- Test контекст переносится в задание
- Test идентификатор следа попадает в аудит
- Test след требует права на журнал
- Test несуществующий след даёт 404
- Test негодный идентификатор следа не роняет
- Test в след не попадают значения настроек
- Test посторонние атрибуты отбрасываются
- Test изменяющие и ошибки пишутся всегда
- Test унаследованное решение уважается
- Test старые следы убираются
- Test свежие следы не трогаются
