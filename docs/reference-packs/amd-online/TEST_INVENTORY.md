# Проверки — Animedia (amd.online)

| Проверка | Где | Статус |
|---|---|---|
| Схема пакета референса | `tests/unit/test_reference_packs.py` | активна |
| Уникальность routeId и blockId | там же | активна |
| Каждый блок документирован | там же | активна |
| Черновик не включает production | там же | активна |
| Черновик не включает индексацию | там же | активна |
| Запрет `latest` | там же | активна |
| Токены соответствуют состоянию доступа | там же | активна |
| Индекс снимков соответствует состоянию доступа | там же | активна |
| Измерение референса | `tests/tools/measure_reference.js` | **отработало** |
| Измерение цвета референса | `tests/tools/measure_reference_colors.js` | **отработало** |
| Происхождение каждого токена | `tests/unit/test_reference_pack_amd_online_measured.py` | активна |
| Матрица поверхностей и ширин полна | там же | активна |
| Дайджест у каждого снимка | там же | активна |
| Цвет измерен в каждой из 15 ячеек | там же | активна |
| Цвет — нормализованный hex, а не название | там же | активна |
| У цвета есть время замера и дайджест снимка | там же | активна |
| Исключения объявлены с причиной и provenance | там же | активна |
| `exclusions_digest` сходится с составом списка | там же | активна |
| Дайджесты файлов доказательств сходятся | там же | активна (пропускается без evidence-каталога) |
| Компонент `colors` не исключён целиком | там же | активна |
| Негативный тест: цвет без provenance отвергается | там же | активна |
| Негативный тест: пустая ячейка цвета отвергается | там же | активна |
| Совместимость с `visual-scoring/1.0.0` | там же | активна (пропускается, если контракт не в дереве) |
| Пакет не объявляет себя сертифицированным | там же | активна |
| В пакете нет чужих ассетов | там же | активна |
| Измерение порядка секций и основной сетки | `tests/tools/measure_reference_structure.js` | **отработало (2026-09-17)** |
| `structure_order` во всех 15 ячейках | `tests/unit/test_reference_pack_amd_online_measured.py` | активна |
| `structure_order` — непустой список ролей, `component` совпадает с именем | там же | активна |
| `structure_order` не одинаков на всех поверхностях без причины | там же | активна |
| `structure_order` подтверждён двумя независимыми проходами | там же | активна (пропускается без evidence-каталога) |
| `grid_columns`/`grid_gap` совпадают с однозначно выбранной основной сеткой | там же | активна (пропускается без evidence-каталога) |
| `header_sticky` остаётся зафиксированным блокером контракта, не переименован | там же | активна (часть — пропускается, если контракт не в дереве) |
| `cards_media` не исключён компонентом целиком | там же | активна |
| Визуальный эталон (алгоритм сравнения) | `contracts/visual-scoring/1.0.0/` | **в этой ветке отсутствует** |

Последняя строка изменилась по существу, и важно не выдать это за большее, чем
оно есть. Алгоритм сравнения написан — контракт `visual-scoring/1.0.0`
подготовлен отдельной веткой (PR #81) — но **в эту ветку он не входит** и здесь
не исполняется. Пакет приведён в соответствие его требованиям и проверен против
него разово, вне CI; результат такой проверки записан в `HANDOFF.md` вместе с
командой и кодом возврата.

Отсюда следует то же, что и раньше: пакет остаётся черновиком, а оценку
кандидата выполняет независимый проверяющий после того, как контракт окажется в
общей ветке. Автор пакета не считает по нему собственный результат.
