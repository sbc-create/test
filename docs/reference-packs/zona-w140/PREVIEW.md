# Локальный просмотр — Zona (w140.zona.plus)

Просмотра пока нет: шаблон не построен, потому что не с чем сверяться.

Когда доступ откроют, порядок такой:

```bash
python3 -m factory reference-audit --ref zona-w140
```

Инструмент измерения готов и не требует правки кода: `tests/tools/measure_reference.js`.
