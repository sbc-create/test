{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/sbc-create/test/schemas/editorial-calendar.schema.json",
  "title": "Editorial Calendar",
  "description": "Per-site editorial calendar. Calendars are keyed by site so that one shared plan cannot produce identical content across the portfolio.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "calendars"],
  "properties": {
    "version": {"type": "integer", "minimum": 1},
    "note": {"type": "string"},
    "calendars": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["site_id", "items"],
        "properties": {
          "site_id": {"type": "string", "pattern": "^[a-z0-9]{2,}(-[a-z0-9]+)*$"},
          "synthetic": {"type": "boolean"},
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["item_id", "title", "rubric", "intent", "state"],
              "properties": {
                "item_id": {"type": "string", "pattern": "^bkl-[a-z0-9-]+$"},
                "title": {"type": "string", "minLength": 1},
                "rubric": {"type": "string", "minLength": 1},
                "intent": {
                  "type": "string",
                  "enum": ["informational", "navigational", "commercial", "transactional"]
                },
                "state": {
                  "type": "string",
                  "enum": ["idea", "ready", "scheduled", "published", "dropped"]
                },
                "scheduled_for": {"type": "string", "format": "date"},
                "priority": {"type": "number", "minimum": 0},
                "rationale": {"type": "string"}
              }
            }
          }
        }
      }
    }
  }
}
