# Production-приёмка NIGHT-03

Документ заполняется по мере снятия проверок; каждая строка — измерение, а не
намерение. Пункты, зависящие от установки быстрого пути, помечены и заполняются
после неё.

## Снято до установки, 2026-09-01 05:20–05:50 UTC

### Шесть витрин отвечают 200

```
lordfilm47.space       200 за 0,082 с
lordserial33.biz       200 за 0,081 с
1lordserials1.online   200 за 0,073 с
yummyani.site          200 за 1,148 с
yummyani.org           200 за 1,975 с
yummyani.biz           200 за 1,955 с
```

### PLAYER_FREEZE_GATE — 30/30

```
lords-01  плеер найден 10 из 10
lords-02  плеер найден 10 из 10
lords-03  плеер найден 10 из 10
ИТОГО: 30/30
```

Критерий гейта — «есть поток, есть плеер». Обратное (нет потока — нет плеера)
отказом не считается: у 672 записей из 53 116 потока нет вовсе.

### Релизы Lords целы

```
lords-01 current=d853747e6413 релизов=2
lords-02 current=7fb4c7c2f2fa релизов=2
lords-03 current=1f9009d02220 релизов=2
```

`lords-02` и `lords-03` переключились циклом, завершившимся в 00:44 —
это штатная работа конвейера, а не следствие моих действий: репозиторий всё это
время оставался на `deploy/02b`.

### Yummy: авторизация восстановлена, свежесть в норме

```
[watchdog] site: индекс свеж (286 с, источников 201)
[watchdog] org:  индекс свеж (267 с, источников 201)
[watchdog] biz:  индекс свеж (250 с, источников 201)
yummy-catalog-watchdog.service: Deactivated successfully.
```

286 секунд при пороге 900 — срок соблюдён с четырёхкратным запасом. Пустой
индекс не публиковался ни разу. Причина отказа и способ восстановления — в
`BLOCKERS.md`, раздел 4.

### Разграничение прав проверено живым запросом

```
release.publish:  viewer/seo/editor/operator/admin → 403,  owner → 409
ingestion.run:    operator → 202,  viewer → 403
без ключа:        любой путь → 401
```

Подробности и оговорки — в `SECURITY.md`.

### Фоновых бесхозных процессов нет

Найден и снят один: `/tmp/wait-lords.sh`, работавший 8 ч 49 мин с 20:41.
Причина, по которой он не завершился сам, — ошибка в нём же:
`alive=$(pgrep -cf ... || echo 0)` при отсутствии совпадений даёт **две** строки
«0» (одну печатает `pgrep -c`, вторую — `echo`), и сравнение `[ "$alive" = "0" ]`
не срабатывает никогда. Из-за этого окно 00:44–03:56 осталось незамеченным.
Сценарий удалён.

Осталось два процесса, оба намеренные и с известным концом: установщик
`install-fast-path.sh` на сервере и ожидание его завершения на моей стороне.
Второй экземпляр CMS canary (порт 8711), поднятый для проверки прав, остановлен,
ключи удалены.

## Установка выполнена, 2026-09-01 13:27:53 UTC

### Первая попытка провалилась — и это хорошо

Установщик дождался окна в 11:59:27 и **отказался** устанавливать:

```
fatal: 'deploy/night-03-fast-path' is already checked out at '/home/claude/work-deploy-fp'
ПРОВАЛ: checkout не выполнен, ничего не изменено
```

Ветка была занята моим же подготовительным worktree — упущение при подготовке,
а не при исполнении. Установщик повёл себя правильно: ничего не тронул и вышел.
Окно 11:59–14:56 при этом осталось открытым, и установка доведена вручную в
13:27 после снятия worktree (чистого, HEAD тот же).

### Частичное переключение и его починка

Первый `checkout` прошёл наполовину:

```
error: unable to unlink old 'factory/site_engine/fingerprint.py': Permission denied
```

HEAD сместился, а один файл остался прежним: он принадлежит `root` (в дереве
таких файлов 144 — следы прогонов от root), а каталог `factory/site_engine`
имеет права `root:root 755`. Состояние опасное: ворота импортируют из
`fingerprint.py` две новые функции, которых в старой версии нет.

Починено восстановлением одного файла из HEAD. После починки — `0` расхождений.

### Что установлено

| Проверка | Значение |
| --- | --- |
| ветка | `deploy/night-03-fast-path` |
| **фактический SHA** | **`788c1a79d51fbfa6b2b1c9d7f6981d047e3f2763`** |
| расхождений рабочего дерева с HEAD | **0** |
| новые слагаемые отпечатка | `enrichment_digest`, `mapping_digest` на месте, частей 12 |
| `bash -n` конвейера | ок |
| компиляция пяти модулей | ок |
| импорт `factory.lords.fast_path` | ок |
| тесты быстрого пути **на установленном коде** | **32 пройдено** |

## Неизменившийся каталог не запускает полный рендер

Проба на установленном коде, от `root`, чтением боевого релиза и кэша; снимок
изолирован в `--repo /tmp/fp-probe`, боевой `var/lords/render-state` не тронут.

```
код возврата: 10   (переписывать нечего)
изменений в каталоге: added 0, modified 0, removed 0
rendered_pages: 0 | добавлено: 0 | удалено: 0
релиз создан: None
база не тронута: True, нарушений 0
секунд: 47,25
```

**rendered_pages = 0**, релиз не создаётся, символьная ссылка не переключается.

## Одно изменение переписывает только зависимые страницы

Изменён постер одного произведения — **в копии кэша**; боевой кэш проверен и не
содержит правки.

```
обнаружено: изменено 1 — uzniki-repeticiya-proschaniya
rendered_pages: 6 из 9260,  64,3 с,  база не тронута
    title/uzniki-repeticiya-proschaniya/index.html   страница произведения
    index.html                                       карусель на главной
    catalog/page/68/index.html                       страница каталога
    new/page/68/index.html                           листинг новинок
    series/page/33/index.html                        листинг сериалов
    years/2026/page/68/index.html                    страница года
```

## Откат отрепетирован в обе стороны

| Шаг | Результат |
| --- | --- |
| `checkout deploy/02b` | HEAD `ad6be19fbf42`, расхождений 0, быстрый путь исчез, конвейер разбирается, три домена 200 |
| `checkout deploy/night-03-fast-path` | HEAD `788c1a79…`, расхождений 0, быстрый путь на месте, импорт ок, три домена 200 |

Откат — переключение ветки, ничего не собирает и не публикует. Второй способ,
не требующий и его: `LORDS_FAST_PATH=0` в окружении службы.

## Таймер

```
active, enabled
следующее срабатывание: 2026-09-01 14:56:06 UTC
```

## Вёрстка и SEO не изменились

Сравнивался не размер страницы — он меняется от содержимого, — а **структура**:
набор CSS-классов и подключаемых стилей на главной и 25 страницах произведений,
между живым релизом и staging, собранным **установленным** кодом.

```
расхождений в CSS-классах и подключаемых стилях: 0
карусель: rail__item 18=18, rail__link 18=18, rail__arrow 2=2, rail__meta 18=18
главная: 48 416 байт против 48 490 у релиза (каталог ушёл вперёд)
robots.txt и sitemap.xml: побайтно совпадают с дозакладочным снимком на всех трёх доменах
```

Промежуточная тревога была моей ошибкой: первую пробу я запустил с пустым
`--var`, и рендерер не нашёл кэшей обогащения и проигрываемости — карусель
пропала. С боевым `var` расхождений нет. Конвейер вызывает быстрый путь **без**
`--var`, то есть со значением по умолчанию `repo/var` — боевым.

## PLAYER_FREEZE_GATE: 28–30 из 30, и промахи — не плеер

Три прогона подряд: **29/30, 30/30, 28/30**. Все четыре промаха проверены
поимённо — каждый отдаёт **404**: страницы произведения в релизе нет вовсе.

```
lords-01  страниц 52 430  записей в каталоге 53 135  не отрисовано 705
lords-02  страниц 52 430  записей 53 135            не отрисовано 705
lords-03  страниц 52 331  записей 53 135            не отрисовано 804
```

Релизы собраны в 06:42, каталог с тех пор вырос. Гейт берёт произведения из
**нынешнего** каталога и ищет их в **опубликованном** релизе, поэтому его
промахи измеряют отставание релиза, а не работу плеера. Ни одной страницы с
потоком и без плеера не найдено.

Отставание — ровно то, что закрывает быстрый путь: эти 705 произведений
следующий цикл отрисует как добавленные, а не пропустит и не пересоберёт всё.

## Место на диске

Мои пробы заняли 3,8 ГБ, и диск дошёл до **99 %** (1,2 ГБ свободно). Пробы
убраны, стало 4,4 ГБ свободно (94 %). Цикл в 14:56 получит место, но запас
остаётся тонким — см. OWNER_ACTION.

## Шесть витрин и свежесть

```
lordfilm47.space 200 / 0,111 с    yummyani.site 200 / 1,726 с
lordserial33.biz 200 / 0,084 с    yummyani.org  200 / 3,391 с
1lordserials1.online 200 / 0,106 с  yummyani.biz 200 / 2,176 с

[watchdog] site 285 с | org 269 с | biz 252 с — при пороге 900
```

## Релизы и процессы

```
lords-01 current=fb7590c76072 релизов=2
lords-02 current=e385d02dfc78 релизов=2
lords-03 current=6def22f83da6 релизов=2
```

Символьные ссылки те же, что до установки. Установщиков — ни одного (завершился),
бесхозных процессов нет; CMS canary на 127.0.0.1:8710 работает намеренно.

## Чего нет

**Трёх настоящих timer-driven циклов.** Первый цикл с установленным быстрым
путём начнётся в 14:56 и будет **полным**: снимка произведений в боевом
`var/lords/render-state` нет, и быстрый путь честно ответит «нужен полный
рендер». При наблюдавшейся длительности 7–9 часов он закончится около 22:00–23:00,
второй — около 01:56, третий — около 12:56 следующих суток.

Записать снимок заранее, чтобы первый цикл стал быстрым, **нельзя**: снимок,
снятый с каталога, который новее опубликованного релиза, объявил бы
неотрисованными те самые 705 произведений отработанными — и они не появились бы
на витрине никогда.

## Что сделает первый цикл — проверено заранее, а не предсказано

Запуск быстрого пути на боевом репозитории, **только на чтение**, без `--record`:

```
$ lords-fast-render.py lords-01 --repo /srv/site-factory/repo --current /srv/lords/lords-01/current/site
код возврата: 2
{"site": "lords-01", "решение": "снимка произведений нет — нужен полный рендер", "titles": 53135}

staging не создан
var/lords/render-state/: каталога нет, файлов 0
```

Значит цикл в 14:56 пойдёт полной сборкой — ровно как до установки, — а снимок
произведений появится только после приёмки релиза. Конвейер обрабатывает код 2
веткой «быстрый путь неприменим — полная сборка»; ни staging, ни состояние при
этом не трогаются, что и подтверждено выше.

## Набор тестов с включённым аудитором

Аудитор записей в боевые каталоги работает в **запрещающем** режиме по
умолчанию. Полный прогон в этом режиме:

```
3311 пройдено, 3 пропущено, 0 упало   (3 мин 37 с)
```

Отдельно, в режиме наблюдения, чтобы увидеть весь список нарушителей, а не
первого: **ноль попыток** записи в `/srv`, `/etc`, `/var/lib`, `/var/cache`,
`/run` за 2870 проверок `tests/unit`.

Границы модулей — 0 нарушений. Линтер по изменённым файлам — чисто.

## PLAYER_FREEZE_GATE после исправления самого гейта: 30/30

Первые прогоны дали 29/30, 30/30, 28/30, и я записал промахи как отставание
релиза. Разобравшись, нашёл, что гейт складывал в один счётчик **три разных
исхода**, и два из них витрины не касаются.

| Исход | Что это | Как было | Как стало |
| --- | --- | --- | --- |
| страницы нет (404) | релиз старше каталога: не отрисовано 705 / 705 / 804 произведения | «БЕЗ ПЛЕЕРА при наличии потока» | отдельный счётчик отставания |
| обрыв на середине тела | заголовки пришли, код 200, разметка усечена | «БЕЗ ПЛЕЕРА» | проверяется код возврата `curl`, считается невыполненной проверкой |
| ложное срабатывание одиночного запроса | страница исправна, но один запрос не нашёл разметку | «БЕЗ ПЛЕЕРА» | отказ объявляется только после перепроверки |

Третий случай стоит отдельного слова. Две страницы, объявленные отказом, я
перепроверил вручную: `izyskannaya-nensi-klensi` и `u-nas-prividenie` отдали
`<video-player>` **12 раз из 12** каждая. То есть отказ был у проверки, а не у
витрины. Гейт, который так делает, обесценивает себя: на исправной витрине он
показывает 28–29 из 30, и на настоящую поломку никто уже не посмотрит.

После исправления — четыре прогона подряд:

```
30/30   30/30   29/29 (+1 страницы нет)   30/30
```

Ни одного ложного отказа. Отдельно, с выборкой по 20 на витрину: **60/60**.

Что это меняет в оценке: **плеер не отказал ни разу за все прогоны**. Прежняя
цифра 28–30/30 измеряла качество гейта, а не витрины.

### Два разных гейта с одним именем — их стоит не путать

`PLAYER_FREEZE_GATE` в конвейере и `automation/host/lords-player-gate.sh` — не
одно и то же, и исправлял я второй.

| | в конвейере | `lords-player-gate.sh` |
| --- | --- | --- |
| что сравнивает | число файлов с `<video-player>` в staging против текущего релиза | живые страницы по HTTP |
| источник данных | файловая система | сеть |
| допуск | 10 % (источник вправе убрать видео у части произведений) | нет |
| может ли заблокировать выкладку | **да** | нет, это моя приёмочная проверка |
| уязвим к обрыву связи | нет | был — исправлено |

То есть ложные срабатывания, которые я нашёл и устранил, выкладку заблокировать
не могли: конвейерный гейт их не видит, он работает по файлам в staging.

Проверено отдельно: быстрый путь конвейерному гейту не мешает. Staging собирается
связанной копией текущего релиза, поэтому страницы произведений в нём все — и
счётчик `grep -rl "<video-player"` по `staging/title` сохраняет прежний порядок
величины, а не падает до числа переписанных страниц.
