# Аудит зависимостей

Сначала измерено, потом классифицировано. Массового переноса файлов не
проводилось: он делает диагноз невозможным.

## Находки

| № | Находка | Доказательство | Классификация |
| --- | --- | --- | --- |
| 1 | Ворота рендера не учитывали обогащение и признак воспроизводимости | сборка без обогащения теряет жанровые фильтры и карусели; `animation/index.html` расходился с релизом | **исправлено сейчас** |
| 2 | Быстрый путь не подключён к конвейеру | `lords-content-refresh.sh` вызывал только полную сборку | **исправлено сейчас** |
| 3 | CMS обращалась к внутренностям пакета `api` | гейт границ: 2 нарушения в `cms/server.py` | **исправлено сейчас** |
| 4 | Проверка границ читала несуществующее поле `violations` | всегда сообщала «0 нарушений», в том числе когда их было 2 | **исправлено сейчас** |
| 5 | Прямых обращений к API поставщика из рендера нет | `grep` по `factory/lords/render.py`: 0 сетевых вызовов | подтверждено, оставлено |
| 6 | Секретов в коде нет | 0 файлов с похожим на токен присваиванием | подтверждено, оставлено |
| 7 | Каталог читают семь мест | `live_site`, `content_live`, `fast_path`, ворота, `completeness`, `recs/cli`, быстрый рендер | **обёрнуто адаптером**: все ходят через `live_site.load_live_items` |
| 8 | `lords → seo` — рендер знает о SEO | 3 импорта | **оставлена совместимость**: направление одностороннее, SEO о рендере не знает |
| 9 | Хостовые сценарии запускаются из чужого worktree | таймеры Yummy читают `/srv/sites/yummyani-staging/repo` — ветка `night-yummy-schedule-12`, тогда как контейнеры на `93ba4d15` | **вынесено в следующую задачу** |
| 10 | Тяжёлая синхронизация внутри HTTP-запроса | устранена в 02F: пересчёт индекса вынесен в отдельный процесс | подтверждено, оставлено |
| 11 | Изображения шли мимо локального кэша | устранено в 02F | подтверждено, оставлено |
| 12 | `test_job_result` требует данных пилота из `var/` | в worktree без `var/` падает, в production-репозитории проходит | **оставлена совместимость**: тест привязан к рабочему каталогу |

## Находка 4 стоит отдельного слова

Проверка границ модулей вызывалась мною так:

```python
нарушений = len(getattr(итог, "violations", ()) or ())
```

Поля `violations` у результата нет — оно называется `problems`. `getattr` с
запасным значением вернул пустой кортеж, и проверка сообщала «0 нарушений»
ровно тогда, когда нарушений было два — причём в коде, написанном в этой же
задаче.

Проверка, которая при любом положении дел отвечает «всё хорошо», хуже её
отсутствия: отсутствие видно, а ложное спокойствие — нет.

## Циклических зависимостей не обнаружено

Карта импортов между пакетами `factory/*` не содержит циклов. Направления:
`lords → paths, seo, errors`, `secret_hub → errors, paths`,
`analytics → errors, paths`, `recs → lords`.

## Неиспользуемого кода не нашлось

Поиск модулей без импортов дал шесть кандидатов из 135. Проверка каждого
показала, что **ни один не мёртв**:

| Кандидат | Кто им пользуется |
| --- | --- |
| `factory.secret_hub.rootcmd` | два shell-сценария, systemd-юнит `site-factory-secret-hub-import@`, тест, `knowledge/DECISIONS.md` |
| `factory.lords.live_bundle` | `automation/host/finalize-public-sites.sh` |
| `factory.topvisor.enroll` | упоминается в сценариях |
| `factory.__main__`, `factory.secret_hub.__main__`, `factory.secret_hub.panel.__main__` | точки входа `python -m` |

Поиск по импортам не видит вызовов из shell и systemd. Удаление «неиспользуемого»
по такому списку сломало бы установку Secret Hub и публикацию сайтов.

Поэтому в этой задаче **не удалено ничего**. Уборка свелась к исправлению двух
настоящих дефектов (слепая проверка границ, нарушение границы в CMS) и к
подключению быстрого пути.

## Дублирование конфигурации

Слои кэша задавались в двух местах — в `scaffold.DEFAULT_CACHE_LAYERS` и в
профилях сайтов. Оставлено как есть: первое — значения по умолчанию для нового
сайта, второе — состояние конкретного сайта. Это не дублирование, а умолчание и
переопределение.
