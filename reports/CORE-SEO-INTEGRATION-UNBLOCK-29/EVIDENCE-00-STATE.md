# Этап 0: состояние трёх контуров, установленное по Git и runtime

Дата: 2026-09-08. Роль: ARCHITECT / CORE. Итерация `CORE-SEO-INTEGRATION-UNBLOCK-29`.

Источником факта здесь служат Git и работающие процессы. Файлы координации
сверялись с ними, а не наоборот.

## Производители

| контур | ветка | полный SHA | дерево | merged | deployed |
|---|---|---|---|---|---|
| CORE | `claude/core-yummyani-route-binding-23` | `e4664da45b67b5b504a47483f0f81719aedae3e7` | чисто | нет | нет |
| SEO | `claude/seo-knowledge-source-rating-28` | `a08469fefaf6c053a64a94866e5dadd3b4609553` | чисто | нет | нет |
| TEMPLATES | `claude/templates-nonlords-visual-26` | `378d2812a25c6cad87ac28a2d42397c88c5425b4` | чисто | нет | нет |

Ни один из трёх не является основной веткой и ни один не выложен. SEO-SHA
`a08469f` — **производитель-кандидат для интеграции**, не `main` и не
выложенная версия.

Интеграционная база этой итерации — CORE `e4664da4`: контракт связи маршрутов
производит CORE, и объединять контуры имеет смысл на стороне производителя.
Ветка `claude/core-seo-integration-unblock-29` создана от неё в отдельном
worktree `/home/claude/wt-core-seo-29`. Боевой checkout не использован.

## Совместимость для SEO API 1.4.0

`COMPATIBILITY_MATRIX.yaml` объявляет `engineContract: 1.2.0` — это контракт
**движка витрин CORE**, и к `seo-engine-api/1.4.0` он отношения не имеет.
Совпадение разрядов в номерах случайное; в матрице версия SEO-API не
объявлена вовсе. Это первый пробел, который итерация закрывает.

Условие совместимости для SEO 1.4.0 формулируется так: потребитель
`seo-route-binding/1.x` совместим, потому что SEO читает весь старший ряд;
операции 1.4.0 добавлены к 1.3.0 аддитивно, и клиент 1.3.0 продолжает
работать. Проверяется прогоном, а не объявлением.

## Замки и блокировки

`locks/RELEASE-BLOCK-b0d0362.json` действует: боевая выкладка control-api
запрещена до трёх условий, снимает только владелец. Итерация выкладку не
выполняет и замка не касается.

## Расхождения источников истины

Записаны, не исправлены: перечисленные файлы принадлежат другим ролям либо
описывают чужую работу.

1. **`status/templates.json` не называет ни ветки, ни SHA.** Объявлена итерация
   `DEPLOY-FIRST-NOW-29` (обновлено 2026-09-08T05:42Z), поля `branch` и
   `headSha` пусты. Фактически worktree `wt-templates-23` стоит на
   `claude/templates-nonlords-visual-26` @ `378d2812`. Что из этого является
   текущим производителем TEMPLATES, по координации определить нельзя.

2. **`PROGRAM_STATE.yaml` отстаёт.** Объявляет текущей итерацией
   `CORE-PLAYER-COVERAGE-AND-WORKER-AUTONOMY-03`; `status/core.json` называет
   `CORE-YUMMYANI-ROUTE-BINDING-23`. Расхождение зафиксировано ещё итерацией
   SEO-28 и не закрыто.

3. **Матрица объявляет управляемыми 7 витрин, SEO обслуживает 6.** Повтор из
   handoff 052. Разбирается в этой итерации отдельно.

## Соседние процессы

На машине активны сборки других направлений (`wt-release-03` — канареечная
сборка lords-02, завершилась к 16:07Z). Чужие процессы не трогались и не
будут: замеры прогона выполняются с проверкой занятости машины.
