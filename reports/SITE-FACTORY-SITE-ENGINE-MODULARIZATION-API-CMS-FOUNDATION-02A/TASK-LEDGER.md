# Журнал задачи 02A

`START_SHA` = `e625d91c9935752af500dc05b35f69cc2fd8a326`
Ветка: `claude/site-engine-modular-api-cms-foundation-02a`
Рабочее дерево: `/home/claude/work-02a` (отдельное, создано под задачу)

## Процессы родительской задачи

Не прерывались, не перезапускались, дубликаты не запускались.

| Процесс | Состояние | Итог |
| --- | --- | --- |
| выкладка Yummy с кэшированием | завершена до начала 02A | код возврата 0; приёмка 60 проверок, 0 неудач; запущен образ `a4476fd` |
| полный рендер Lords, 53 116 записей | идёт с 13:45:59 UTC | обход завершён, рендер первой витрины продолжается; см. `BLOCKERS.md`, РИСК-РЕНДЕР |
| наблюдение за рендером | перезапущено после истечения срока | контроль без блокирующего ожидания |

Результаты выкладки и замеры записаны в родительской ветке коммитом `e625d91`.

## Выполнено

| № | Этап | Итог |
| --- | --- | --- |
| 1 | базовый прогон тестов в новом дереве | 2 826 прошло, 5 пропущено |
| 2 | инвентаризация кода разбором AST | 35 областей, 97 рёбер, 2 взаимные зависимости |
| 3 | карта зависимостей и классификация нарушений | `DEPENDENCY-MAP.md`, ничего не удалено |
| 4 | реестр модулей как данные | 18 модулей: 6 реализовано, 3 адаптировано, 9 контрактом |
| 5 | нормализованная модель | `contracts.py`, 21 проверка |
| 6 | гейт границ | 7 правил, падает на 7 сломанных фикстурах |
| 7 | адаптеры Lords и Yummy | проверены на 53 116 и 6 774 живых записях |
| 8 | загрузчик и дельта серий | обрыв каталога помечается, а не скрывается |
| 9 | абстракция кэша | 18 проверок; production не тронут |
| 10 | API v1 | 11 маршрутов, выключен по умолчанию, 27 + 7 проверок |
| 11 | редакционный слой CMS | 11 моделей, 18 проверок, ни одного изменяющего маршрута |
| 12 | ADR по основе CMS | рекомендация: гибрид |
| 13 | новый род сайта | `demo-books` проходит оба гейта без копирования |
| 14 | полный прогон тестов | 2 976 прошло, 5 пропущено, 0 упало |

## Найдено по ходу и исправлено

| Что | Как обнаружено |
| --- | --- |
| адаптеры поставщика и рендерера в одном файле — границы между ними нет | гейт границ на моём же коде |
| проверка слов читала комментарий как поведение | гейт границ на моём же коде |
| правило «импорт внутренностей» не могло сработать никогда | разбор при написании теста |
| правило гейта требовало `provider-adapters` у сайта без поставщика | сборка профиля нового рода |
| схема требовала непустой список поставщиков | то же |
| адаптер Yummy отдавал пустые сезоны вместо счётчиков | прогон на живом снимке |
| адаптер Lords терял идентификатор Кинопоиска | прогон на живом кэше |
| проверка без `assert` | ruff |
| ожидание «ровно шесть профилей» считало не то свойство | полный прогон |
| утверждение об отсутствии `updated_at` было шире действительности | разбор снимка |

## Не сделано и почему

| Что | Причина |
| --- | --- |
| перенос `build`/`validation` с `factory.lords` на реестр рендереров | 02A запрещает смешивать рефакторинг с продуктовыми изменениями; реестр готов, перенос — в `02B` |
| разрыв связи `lords.gate → factory.seo` | требует стыковки с SEO, а её нет |
| изменяющие маршруты CMS | нет настоящей авторизации |
| промышленное хранилище | `InMemoryStore` достаточен для каркаса; интерфейс готов |
| гейт границ для TypeScript | заготовка на одно правило; витрины в соседнем репозитории |
