# Результаты тестов

## Полный набор

| Момент | Прошло | Пропущено | Упало |
| --- | --- | --- | --- |
| до изменений (базовый прогон в новом дереве) | 2 826 | 5 | 0 |
| после всех изменений | 2 987 | 5 | 0 |

Прирост — 161 новая проверка. Ни одно существующее ожидание не ослаблено ради
зелёного результата.

Пропуски — те же пять, что и в базовом прогоне: два о версии интерпретатора,
два об отсутствии `.venv` в этом дереве, один о закрытом каталоге секретов.
Последний пропуск намеренный: состояние ключа не измерено, и тест это говорит,
а не притворяется пройденным.

## Новые проверки по частям

| Набор | Проверок | Чему |
| --- | --- | --- |
| `test_site_engine_contracts.py` | 21 | нормализованная модель |
| `test_site_engine_boundaries.py` | 20 | гейт границ, включая десять сломанных фикстур |
| `test_site_engine_store.py` | 19 | хранилище и профили |
| `test_site_engine_cache.py` | 20 | контракт кэша |
| `test_site_engine_adapters.py` | 25 | адаптеры, дельта серий, прогон обхода |
| `test_site_engine_api.py` | 30 | контракт API |
| `test_site_engine_editorial.py` | 18 | редакционный слой |
| `test_site_engine_openapi.py` | 7 | сверка описания с поведением |

## Одно изменённое ожидание, с обоснованием

`TestSixLiveProfiles::test_all_six_sites_have_a_profile` требовал ровно шести
профилей. Появление профиля нового рода сайта (`demo-books`) ничего не говорит о
шести действующих, а проверка падала — то есть считала не то свойство, ради
которого писалась. Теперь она проверяет, что ни один действующий сайт не
остался без профиля.

Зубы прежней проверки не потеряны: добавлен
`test_no_extra_profile_claims_a_live_domain`, отклоняющий недействующий профиль,
который заявил бы домен работающего сайта.

## Что ловили тесты по ходу работы

Не всё это придумано заранее — часть нашлась при первом же прогоне.

* **Гейт границ уронил мой собственный код** трижды. Два нарушения оказались
  настоящими (адаптеры поставщика и рендерера в одном файле), одно —
  неточностью самой проверки (слово в комментарии читалось как поведение).
* **ruff нашёл проверку, которая ничего не проверяла**: строка
  `тайтл.provider_id == ...` без `assert`. Тест выглядел зелёным, потому что
  ничего не утверждал.
* **Проверка на живых данных нашла две ошибки в адаптерах**, невидимые на
  фикстуре: фикстуру пишет тот же, кто пишет адаптер, и она повторяет его
  заблуждение.

## Что нашёл повторный обзор собственного кода

Проведён после того, как основной объём был готов, — с установкой читать код как
чужой. Нашлось три дефекта, каждый того же рода, о котором я предупреждаю в
других местах.

**Кэш блокировал посетителя в окне фонового обновления.** Загрузчик вызывался
прямо в этой ветке, и получалась худшая из возможных: посетитель ждал источник и
всё равно получал устаревшее значение. Мой собственный тест этого не поймал — он
сверял только отданное значение, то есть подтверждал реализацию, а не свойство.
Проверено измерением: ожидание совпадало с задержкой источника до миллисекунды
(0,401 с при источнике в 0,400 с). После правки — 0,001 с. Новый тест измеряет
время, а не читает код.

**Полки собирались из первой сотни записей.** «С высокой оценкой» означало
«лучшее из первой сотни» — при каталоге в 53 тысячи записей неверный ответ,
выглядящий правдоподобно. Тест кладёт лучшую оценку на 250-ю позицию и требует
найти её; ответ теперь сообщает, сколько записей просмотрено.

**Несуществующий профиль пропускался молча.** Собрать API меньшего состава, чем
просили, и не сказать об этом — тот же род ошибки. Теперь отказ.

## Наблюдение о нестабильности

`tests/integration/test_rollback.py::test_previous_release_is_kept` однажды
упал при полном прогоне, шедшем одновременно с рендером Lords. В отдельном
прогоне и во всех последующих полных прогонах проходит. Похоже на состязание за
раскладку релизов с работающим рендером. Записано, а не забыто: необъяснённое
падение, о котором смолчали, возвращается в худший момент.

## Проверка линтером

`ruff check factory/ tests/ tools/` — чисто.
