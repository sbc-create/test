# CORE-CONTENT-KIND-CONTRACT-33 — итог

| | |
|---|---|
| TASK_STATUS | **DONE** |
| BASE_SHA | `1f29ed5a8d7c1f56f928d35171cedc548a615f01` |
| FINAL_SHA | `122652572e18e3c44b915bc4185512ac51cb076a` |
| BRANCH | `claude/core-content-kind-contract-33` |
| MERGED / DEPLOYED | нет / нет |
| CONTRACTS | `core-route-snapshot/1.1.0`, `core-seo-bridge/1.1.0` |
| TESTS | **4579 passed, 0 failed, 3 skipped** (8:37) |
| EXTERNAL_REQUESTS | 0 |
| PRODUCTION | не изменялось |
| HANDOFFS | 060 в SEO; 058 закрыт |

## Результат одним числом

**Пригодных записей у потребителя: было 0 из 47 684, стало 44 245 из 47 692.**
Проверено кодом SEO без единой правки.

Остальные 3 447 удержаны по столкновению адресов — это наш `slugify`, и это
следующая работа.

## Пять дефектов

| | закрыто |
|---|---|
| Д1 `producerSha` | полный SHA, проверка **у производителя**; ограничение объявлено схемой |
| Д2 отпечаток | добавлен `envelopeDigest` на заголовок |
| Д3 время | `sourceObservedAt` объявлен, порядок проверяется при сборке |
| Д4 **вид произведения** | обязателен, умолчания нет; мост считает **той же функцией** |
| Д5 правила | `sourceDigestAlgorithm` и `normalizationVersion` объявлены |

Причина Д1 лежала глубже поля: **объявленной схемы у контракта не было
вовсе**, ограничить длину было негде. Схема написана.

## Про вид: что даём и чего не даём

`type` и `is_series` сходятся на **всех 53 310 записях без единого
расхождения**. Расхождение теперь объявляется `CONFLICT`, а не сглаживается.

Аниме каталог назвать не умеет — `kindTaxonomy: "core-catalog/type:2"`
объявлен прямо в снимке. То, что каталог всё же помечает, идёт отдельными
осями: `contentForm` у 320 записей, `isAnimation` у 367.

**`isAnimation` не бывает `false`.** Теги заполнены у 2,4 % записей;
отсутствие метки значит, что запись не помечали. Вернуть `false` значило бы
превратить наше молчание в утверждение о мире.

## Что нужно от SEO: одна строка

Потребитель отвергает `1.1.0` с `SCHEMA_UNKNOWN`. Он объявляет
`COMPATIBLE_MAJOR = "1"`, но принимает только точное `1.0.0`. Мы не стали
называть новый контракт прежним номером, чтобы это обойти: это была бы ложь
ради прохождения чужой проверки.

## Ворота поймали меня четырежды

Чистота ядра — словарь витрины внутри универсального модуля, **второй раз за
две итерации**. Реестры — файл настройки без схемы. Схемы — схема без фикстур.
И оснастка проверок, несшая `producer_sha` восемью знаками: она воспроизводила
ровно ту ошибку, которую потом нашёл потребитель, и потому о ней молчала.

Каждый раз чинил код, а не ворота.

## Источник сдвинулся

Каталог обновлён в `13:15:35Z`: 53 296 → 53 310 записей, маршрутов
47 684 → 47 692, столкновений 3 443 → 3 447. Новое наблюдение, а не пересчёт
прежнего.
