# База итерации 33

| | |
|---|---|
| роль | CORE / ARCHITECT |
| BASE_SHA | `1f29ed5a8d7c1f56f928d35171cedc548a615f01` |
| ветка базы | `claude/core-lords-route-snapshot-31` |
| новая ветка | `claude/core-content-kind-contract-33` |
| дерево базы | чисто, 0 изменений |
| remote | `origin` → sbc-create/test.git |
| Python | 3.10.12 — `StrEnum` и `dt.UTC` недоступны |
| потребитель | SEO `6800196a6146ceb786b416e1123a3ccfce4bca7b` |

## Что закрываем

Handoff `058-SEO-TO-CORE` — пять дефектов производителя, найденных
потребителем на настоящем артефакте:

| | дефект | тяжесть |
|---|---|---|
| Д1 | `producerSha` не ограничен: принят сокращённый SHA базы | BLOCKING |
| Д2 | отпечаток покрывает только записи; заголовок подменяем незаметно | BLOCKING |
| Д3 | `observedAt` объявлен раньше метки снятия источника | P2 |
| Д4 | **вида произведения нет ни в снимке, ни в мосте** | BLOCKING |
| Д5 | правило вычисления `sourceDigest` не объявлено | P3 |

Д4 — главный. Из-за него у потребителя **ноль годных записей из 47 684**:
связь маршрутов решена, а следующие ворота пусты. Каталог поле имеет —
`type` заполнен у всех 53 296 записей и сходится с `is_series`.

## Найдено при разборе базы

Объявленной схемы у `core-route-snapshot` **нет вовсе**: контракт живёт
только в коде. Отсюда и Д1 — ограничить поле было негде, и `build()` берёт
`producer_sha` параметром, ничего о нём не проверяя.

Это не отдельный дефект, а причина первого: контракт, существующий только
кодом, нельзя ни объявить, ни проверить со стороны.
