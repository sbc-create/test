# CYCLE-00 — Инвентаризация Admin Fleet

Этап `ADMIN-FLEET-5-CYCLIC-22`, начало — `463fe108c7fb5d2afb98db32c42744d47e1b2eab`
(перепроверено: дерево чисто, upstream совпадает).

## Пять семейств найдены, а не названы

Перечень взят из `tenant.theme` в `schemas/site-package.schema.json` — той самой
схемы, по которой пакет потом проверяется. Свой список означал бы, что адаптер
находится для семейства, которого схема не знает.

| Семейство | Сайты | Blueprint | Домены | Живой |
| --- | --- | --- | --- | --- |
| `portal_light` | site-a | payload-next-multisite | site-a.localhost | нет |
| `pulse` | site-b | payload-next-multisite | site-b.localhost | нет |
| `editorial` | site-c | payload-next-multisite | site-c.localhost | нет |
| `lords_dark` | lords-01, lords-02 | lords | lordfilm47.space, lordserial33.biz | **да** |
| `lords_light` | lords-03, lords-04 | lords | 1lordserials1.online | **да** (lords-04 без домена) |

Матрица целиком — `FLEET-MATRIX.json`, построена скриптом из профилей и пакетов.

## Базовые уровни, зафиксированные раздельно

* **CENTRAL ADMIN — 8.** Тринадцать разделов, два движка. На флот не переносится.
* **ADMIN FLEET 1–5 — 0 у всех пяти.** Ноль по факту, а не по строгости: своей
  админки у сайта нет вовсе. Вход одной точкой `/admin`, витрина выбирается
  параметром запроса, а каталог операторов не знает слова «сайт» — в
  `operators.py` не было ни `site_id`, ни `tenant_id`. Единственный контур, уже
  разделённый по сайтам, — публичные учётные записи.
* **TEMPLATES 1–5** — по `status/templates.json`: lords `pass`, yummy build и
  live `blocked`, collections `partial`, кандидат `conditional`.
* **SEO** — `SEO-PAGE-QUALITY-AUTODRAFT-21`, пять ворот `DONE`.
* **PRODUCTION** — `claude/day05-lords-merged @ 568863d`, с веткой не синхронизирована.

## Три дефекта инвентаризации

| # | Severity | Что |
| --- | --- | --- |
| FLEET-INV-001 | P1 | **Три боевые витрины вне семейств.** `yummyani-site/org/biz` имеют профиль движка и живые домены, но не имеют пакета и не объявляют `theme_ref`. Их семейство не определено ничем, кроме темы движка `yummy-portal`, которой нет в перечне схемы. |
| FLEET-INV-002 | P2 | `demo-books` тоже без пакета. |
| FLEET-INV-003 | P2 | **Столкновение номеров handoff**: два файла с номером 026 и два с 028 (один написан этим потоком, другой — соседним через девять часов). Нумерация общая, выдача ничем не защищена. Ссылаться следует полным именем файла. |

FLEET-INV-001 существенен для цели: пять семейств из схемы не покрывают три
живые витрины. Приёмка «5/5» по схеме не сделает эти три сайта принятыми, и
объявлять их принятыми нельзя.
