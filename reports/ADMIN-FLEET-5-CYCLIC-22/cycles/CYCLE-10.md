# CYCLE-10 — Пятое семейство и полная приёмка 5/5

## Воспроизведённый пробел

Три семейства из пяти стояли на 7 из-за отсутствия содержимого: `site-a`,
`site-b`, `site-c` не имели каталога, поэтому шаги «разобрать → утвердить →
опубликовать → снять» проверить было нечем. Блокер был передан TEMPLATES
(handoff 032) и ждал чужой поставки.

Пробел оказался уже, чем выглядел. Пакет каждой из трёх витрин **сам
объявляет** источник:

```yaml
content_source:
  kind: fixture
  provenance: Синтетические записи стенда, созданные внутри репозитория фабрики
  rights_confirmed: true
```

То есть содержимое этих витрин по договору и есть набор стенда, созданный
внутри фабрики. Не хватало не разрешения и не поставщика, а самого файла:
`content_package_ref: null`.

## Что сделано

`tests/tools/fixture_catalog.py` создаёт набор **в корне стенда**, а не в
репозитории. Пакет живой витрины принадлежит TEMPLATES, и дописывать его
отсюда значило бы разойтись с открытой поставкой: handoff 032 остаётся в силе,
и когда набор придёт, он заменит стендовый, а не столкнётся с ним.

Три свойства набора проверяются, потому что каждым из них он мог бы соврать:

* **отпечаток объявлен и сходится** — иначе подменённый набор попал бы на
  витрину молча, и разница нашлась бы по виду страниц, а не по отказу;
* **у каждой записи свой идентификатор** — выдуманный не сопоставить ни с чем;
* **в данных есть настоящее расхождение** — вид от поставщика и вид из тега
  из разных групп. Без него очередь разбора пуста, и зелёная приёмка доказала
  бы только то, что решать было нечего.

Первая попытка дала пустую очередь: расхождение было объявлено в названии
(«Эпизод 6»), а вид определяется по типу и тегам. Вторая — тоже пустая: тег
`ova` и тип `tv` лежат в одной группе, и тег лишь уточняет тип. Спор возник
только на паре из разных групп — `movie` против `ova`: **12 записей из 60**.

Записи не выдают себя за поставщика: внешние идентификаторы только `stand`,
воспроизведение `null`. Набор стенда, попавший в соединение как настоящий,
испортил бы связь каталогов.

## Доказательства

`tests/unit/test_fixture_catalog.py` — 7 проверок, включая последнюю:
**пакет витрины в репозитории не тронут** (`content_package_ref` по-прежнему
`null`, каталога `sites/site-a/content` нет).

Полная приёмка флота, оба движка, все пять семейств:

| Семейство | Витрина | chromium | firefox |
| --- | --- | --- | --- |
| `portal_light` | site-a | 0 провалов | 0 провалов |
| `pulse` | site-b | 0 провалов | 0 провалов |
| `editorial` | site-c | 0 провалов | 0 провалов |
| `lords_dark` | lords-01 | 0 провалов | 0 провалов |
| `lords_light` | lords-03 | 0 провалов | 0 провалов |

Каждый прогон — один и тот же путь: вход по адресу витрины, ссылки только
внутрь своего контура, сессия не переходит на соседний сайт, правка настройки
с предпросмотром и откатом, соседняя витрина не задета, разбор → решение →
утверждение вторым человеком → публикация → проверка витрины → снятие,
подделанная форма 403, чужой оператор не виден, ширины 390/768/1440 и
увеличение 200 %, первый Tab попадает на управляемый элемент, у читателя нет
отправляемых форм.

Набор: **4418 passed, 3 skipped, 0 failed**. `ruff check factory tests` чист.

## Найденное попутно

`ruff check .` по всему дереву находит четыре нарушения вне контура: три E501
в `artifacts/evidence/playback/measure-coverage.py` (чужой файл, каталог
`artifacts/**` не входит в список послаблений E501). Не трогаю: механическая
правка чужого файла столкнётся с его потоком. Ворота контура —
`ruff check factory tests` — чисты.

## Поправка владельца от 2026-09-06

Оценка 8 за `portal_light`, `pulse` и `editorial` была выставлена по зелёной
приёмке на наборе стенда. Владелец поправил, и поправка верна: набор стенда
доказывает путь администратора, но не работу витрины на настоящем содержимом,
а закрытость каркаса не переносится на семейства шаблонов.

* приёмка на реальных сайтах — **2 из 5** (`lords_dark`, `lords_light`);
* приёмка на наборе стенда — **3 из 5** (`portal_light`, `pulse`, `editorial`);
* оценки трёх семейств возвращены к **7**, статус флота — **PARTIAL**;
* кандидат `b0d0362` **заблокирован** от production замком
  `coordination/v1/locks/RELEASE-BLOCK-b0d0362.json`.

Снятие блокировки — только владельцем и только после того, как: придут наборы
содержимого по handoff 032; каждое из пяти семейств пройдёт приёмку на реальном
сайте в обоих движках; завершится `CORE-YUMMYANI-ROUTE-BINDING-23`, ветки будут
слиты, и тесты изоляции пройдут **после** слияния — расхождение веток проходит
ровно по тем файлам, где живёт проверка тенанта.
