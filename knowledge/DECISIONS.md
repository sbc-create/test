# DECISIONS — архитектурные решения фабрики

Формат: решение → обоснование → следствие в коде. Детальные ADR — в `adr/`.

| ID | Решение | Обоснование | Реализация |
|----|---------|-------------|------------|
| D1 | Controller на Python 3.11 без внешних фреймворков; единственная обязательная внешняя зависимость — `jsonschema` (валидация) и `PyYAML` (инвентарь). | Python 3.11 и PyYAML уже есть в среде (F2.2); минимум зависимостей = минимум supply-chain риска и воспроизводимость. `pydantic` не установлен и не обязателен: семантическая валидация всё равно доменная. | `factory/`, `requirements.txt` |
| D2 | Состояние очереди и job'ов хранится на диске (`var/state/*.json`, `queue/*`), а не в контексте модели. | Требование §7: Claude не является базой данных; worker должен переживать restart. | `factory/state.py`, `factory/queue.py` |
| D3 | Все производственные мутации выполняются только через `factory deploy`, который сам проверяет manifest, allowlist, авторизацию, backup, lock и quality gates. Claude не имеет права на произвольный SSH. | §5.5. Реализовано двумя слоями: PreToolUse-hook (детерминированный запрет) + deny-правила в settings.json. | `.claude/hooks/guard_bash.py`, `guard_write.py`, `guard_rules.py`, `.claude/settings.json` |
| D4 | Deployment-слой — декларативный, с двумя целевыми адаптерами: `ssh_ansible` (production/staging по SSH) и `local_disposable` (пилот и тесты). Выбор адаптера — из inventory, не из кода. | §4: «Ansible или другой обоснованный декларативный SSH deployment слой». Ansible в среде отсутствует (F2.4), поэтому playbook'и написаны и синтаксически проверяемы, а исполнение помечается `BLOCKED_ACCESS` до установки Ansible и передачи хоста. | `factory/targets/`, `automation/ansible/` |
| D5 | Атомарные релизы: `releases/<build_id>` + симлинк `current`, переключаемый только после health check; предыдущий релиз сохраняется. | §8.7, требование отката. | `factory/targets/base.py`, `automation/ansible/roles/dle_release` |
| D6 | Секреты только через `secret_ref` (имя переменной окружения или путь в secret-хранилище). Значения секретов никогда не попадают в git, лог, отчёт, скриншот и prompt. | §6. Проверяется юнит-тестами редактора и линтером артефактов. | `factory/redaction.py`, `tests/unit/test_redaction.py` |
| D7 | Пути DLE, требующие записи, НЕ угадываются. Профиль путей — обязательный входной файл `blueprints/dle20/profiles/paths.yaml`, заполняемый из официальной документации/дистрибутива. Без него установка даёт `BLOCKED_INPUT`. | §3.8 прямо запрещает угадывание; официальная документация заблокирована egress-политикой (F3.3). | `blueprints/dle20/profiles/paths.template.yaml`, `factory/blueprint.py` |
| D8 | Проверка лицензии — по домену второго уровня (eTLD+1 по встроенному списку публичных суффиксов проекта), до любой production-мутации. Нет покрытия → `BLOCKED_LICENSE`, установка не начинается. | §3.4 и F5.2. | `factory/licensing.py`, `inventory/dle-licenses.yaml` |
| D9 | SEO-контур — часть архитектуры: матрица индексируемости строится ДО шаблонов и является входом для рендера и для acceptance gates. Шаблон, тип страницы которого отсутствует в матрице, не рендерится. | §9A. | `knowledge/SEO_INDEXABILITY_MATRIX.yaml`, `factory/seo/` |
| D10 | Пагинация: серверные `<a href>`-ссылки обязательны; «Показать ещё»/infinite scroll — только progressive enhancement поверх них; каждая страница имеет self-canonical; page 1 имеет один канонический URL. Детерминированный порядок с tie-breaker по `id`. | §9A. Проверяется краулером и E2E. | `themes/*/templates/`, `factory/seo/lint.py` |
| D11 | Контент никогда не генерируется фабрикой. Отсутствующий обязательный контентный факт (в т.ч. `alt`) блокирует публикацию конкретного материала, а не заменяется значением по умолчанию. | §9, §13. | `factory/validation.py` (`BLOCKED_INPUT`, `BLOCKED_RIGHTS`) |
| D12 | VK-плеер и рекламный контур реализованы как адаптеры с явным contract-ref. Без переданного contract доступен только `mock`-адаптер и только для `environment: staging`; в production он технически недоступен. | §9, §11. | `factory/vk.py`, `factory/ads.py`, `tests/unit/test_vk_ads_gating.py` |
| D13 | Пилот выполняется на disposable local target (PHP built-in server), потому что staging-хост не передан, а Docker-демон недоступен (F2.5). Пилот проверяет весь SEO/QA-контур на реальном HTTP. | §11.10. | `factory/targets/local_disposable.py` |
| D14 | Fixture-пакеты помечаются `fixture: true` и отвергаются валидатором при `environment: production` — тестовые данные не могут попасть в production. | §10 «в production попали test/demo данные». | `schemas/site-package.schema.json`, `factory/validation.py` |
| D15 | CDN Video Hub не интегрируется. Создана версионируемая extension point и место под knowledge pack, обе — no-op до второго change request. | §11. | `plugins/cdnvideohub/`, `knowledge/cdnvideohub/.gitkeep` |
| D16 | Идемпотентность достигается контентной адресацией сборки (`build_id` = SHA-256 от нормализованного пакета + blueprint) и проверкой уже применённого релиза перед мутацией. | §3.11, §7. | `factory/build.py`, `factory/targets/base.py` |
| D18 | Конфигурация защиты (`.claude/`) и боевой инвентарь (`ssh-hosts`, `dns-zones`, `dle-licenses`) закрыты для записи агентом. Сильную гарантию даёт файловая система и permission-правила; hook — второй слой. | Независимый security review показал: правило, которое можно переписать тем же механизмом, который оно охраняет, защитой не является. | `.claude/hooks/guard_rules.py`, `test_guard_rules.py::test_guard_configuration_protects_itself` |
| D19 | Разбор команд рекурсивный: обёртки (`env`, `flock`, `watch`, `setsid`), `-c` интерпретаторов, `$(...)`, обратные кавычки, `find -exec`, контейнерные раннеры. Секретные пути запрещены в любой команде, а не только у известных «читалок». | Проверка по первому токену пропускала `env ssh prod`, `bash -c 'ssh …'`, `python3 -c "open('.env')"`. | `.claude/hooks/guard_rules.py`, `BYPASS_ATTEMPTS` в тестах |
| D20 | Ворота production (авторизация, лицензия, пригодность цели) выполняются ДО сборки, а сама авторизация проверяется семантикой, а не схемой. | Схема с `production_authorized: const true` делала статус `BLOCKED_AUTHORIZATION` недостижимым: пакет отклонялся раньше как `BLOCKED_INPUT`, и оба гейта можно было удалить, не уронив ни одного теста. | `factory/pipeline.py`, `tests/unit/test_production_gates.py` |
| D21 | Прерванное задание не возобновляется с середины: оно уходит в `QUARANTINED` с причиной. | Возобновление с произвольного состояния требовало бы отката частично применённых шагов; безопаснее перезапуск с новым job_id. | `factory/pipeline.py`, `test_pipeline_failure_modes.py` |
| D22 | Артефакты проверок изолируются по `job_id`; невыполненная проверка несёт `exit_code: null` и `passed: false`. | Общий каталог перетирался следующим прогоном, а синтетический `exit_code: 0` у незапущенной команды делал отчёт недостоверным. | `factory/verify.py`, `schemas/job-result.schema.json` |
| D23 | Sitemap собирается всегда, публикуется только в production; его содержимое проверяется линтом независимо от окружения. | На staging он не собирался, поэтому правило «только canonical+indexable+200» не проверялось ни разу. | `factory/render.py`, `factory/seo/lint.py` |
| D24 | Замечания уровня major имеют бюджет (`acceptance.max_major_findings`). | Без бюджета major-находки не влияли ни на что, и «20 major» выглядели как полностью зелёная приёмка. | `factory/verify.py` |
| D17 | Requirements traceability: каждое нормативное требование мастер-промпта получает ID `REQ-*` и связывается с тестом. | Требование «отчёт не должен утверждать проверку, которая не запускалась». | `docs/MASTER_PROMPT_REQUIREMENTS.md`, `tests/test_traceability.py` |
| D25 | Второй blueprint `payload-next-multisite` добавлен рядом с DLE, а не вместо него. Пакет выбирает blueprint полем `blueprint`; пакет без поля считается DLE-пакетом и сохраняет прежние ворота. | Три домена на DLE дают три лицензионных контура и три админки; задание требует одной CMS. | `adr/0006-payload-next-multisite.md`, `schemas/site-package.schema.json` |
| D26 | Изоляция сайтов выражается констрейнтом запроса и держится двумя независимыми слоями: собственный access и plugin-multi-tenant. | Фильтрация после выборки утекает через count, Local API и связанные поля; один слой невозможно проверить мутационно. | `src/access/index.ts`, `tests/tools/mutation_isolation.py` |
| D27 | Общий фактический каталог не ссылается на медиатеку сайта: постеры вынесены в отдельную общую коллекцию `catalog-media`. | Общий тайтл со ссылкой на файл одного сайта утёк бы на остальные два. | `src/collections/catalog.ts` |
| D28 | Публичные страницы читаются только через `tenantFind`, который добавляет констрейнт сайта принудительно; анонимный REST закрыт. | `overrideAccess` при серверном рендере отключает фильтр; забытый констрейнт означал бы выдачу чужого сайта. | `src/lib/tenant-query.ts`, `tests/tenant-isolation.ts` |
| D29 | Ворота `cross_site_uniqueness` сравнивают фактически отданные страницы, а близость текста измеряется containment по 4-словным сочетаниям. | Jaccard пропускает «тот же текст плюс абзац»; ворота нашли реальные дубли в собственном стенде. | `factory/seo/uniqueness.py`, `tests/unit/test_cross_site_uniqueness.py` |
| D30 | У раздела-списка есть явный список владельцев: каталог и расписание — по одному сайту, подборки — два, лента материалов — три (у каждого свой заголовок и своё содержание). Не владеющий сайт держит раздел как навигацию с noindex. Состав владельцев зафиксирован тестом. | Три сайта, индексирующие один и тот же список одних и тех же карточек, — это три копии. | `src/seo/profiles.ts` |
| D31 | Контракт Content API не зашит в код: адаптер управляется описанием из `knowledge/cdnvideohub/content-api.yaml`, а без него отвечает BLOCKED_INPUT. | Переданный knowledge pack намеренно не содержит endpoint и полей; придумывать их запрещено. | `src/content-api/descriptor.ts`, `tests/content-api.ts` |
| D32 | Кнопка «Повторить» у плеера меняет ключ монтирования и пересоздаёт элемент. | Снятие текста ошибки без перезапуска — имитация retry; браузерный тест проверяет появление нового экземпляра. | `src/components/Player.tsx`, `tests/e2e-multisite/player.spec.js` |
| D33 | Комментарий принимается только серверным endpoint с подписанным токеном формы, ловушкой, минимальным временем заполнения, лимитом частоты и ключом идемпотентности; прямое создание через REST закрыто. | Открытое создание позволяет обойти любую из этих проверок одним POST. | `src/comments/`, `tests/comments-policy.ts` |
| D34 | Read-only измерение референса выполняется командой фабрики с allowlist из `inventory/reference-sources.yaml`, а не произвольным запросом агента. | Сеть в CLOSED_WORLD открывается только для источника, разрешённого владельцем, и только на время измерения. | `factory/reference.py`, `inventory/reference-sources.yaml` |
| D35 | Независимое измерение amd.online не выполнено: прокси среды отклоняет CONNECT (ERR_TUNNEL_CONNECTION_FAILED). Переданные владельцем наблюдения записаны как непроверенные. | Оценка на глаз вместо измерения сделала бы отчёт недостоверным. | `docs/reference/AMD_ONLINE_UI_AUDIT.md`, `artifacts/reference/amd-online-run.json` |
