[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    slow: тесты, поднимающие сервер или браузер (запускаются в полном прогоне)
    browser: тесты, требующие Chromium
