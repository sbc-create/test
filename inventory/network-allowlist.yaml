# Хосты интеграций, к которым профиль UNATTENDED_SAFE разрешает обычные запросы
# без подтверждения оператора: CDNVideoHub, CMS, VK, аналитика.
#
# Список пуст не по ошибке: контракты и адреса интеграций не переданы. Пустая
# запись означает BLOCKED_INPUT, а не «разрешено всё» — до появления записи
# любой внешний запрос уходит на подтверждение. Расширять список по инициативе
# агента запрещено: каждая строка приходит из задания владельца.
schema_version: 1
hosts: []
# hosts:
#   - ref: cdnvideohub
#     host: api.example.tld            # реальный адрес приходит от владельца
#     purpose: "плеер: получение embed по контракту"
#     methods: [GET]
#     token_secret_ref: env:CDNVIDEOHUB_API_TOKEN
