"""Переносимая ячейка сайта: один домен — один сайт — один репозиторий.

Пакет отвечает за то, чтобы сайт можно было забрать с фабрики целиком и
поставить на отдельный сервер: закреплённый шаблон, отдельный Git-проект,
готовый релиз с digest, собственные данные и штатный перенос.

Общий код при этом остаётся здесь. В ячейку уходит выбранный шаблон, набор
модулей и закреплённые версии — не копия фабрики.
"""
