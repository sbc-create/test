"""Этапы жизненного цикла сайта: общий словарь, а не чья-то собственность.

Перечень жил в `factory/build.py`, а нужен был и стенду
(`factory.lords.preview`), чтобы отличить «собрать нечем» от «выкатывать
некуда». Один этот импорт держал цикл `lords ↔ build`, а цикл означает, что ни
одну из подсистем нельзя вынести, не вынеся вторую.

Перечень не принадлежит сборке: это общий словарь, которым пользуются обе
стороны. Место ему в опоре, которую знают все и которая не знает никого.
"""
from __future__ import annotations

#: Этапы, наступающие после сборки. Штатная сборка останавливается на любом
#: блокере и этот список не смотрит — он нужен стенду, чтобы отличить «собрать
#: нечем» от «выкатывать некуда». Смягчения здесь нет: блокер этапа выката
#: по-прежнему закрывает выкат.
POST_BUILD_STAGES = frozenset({
    "STAGING_DEPLOY", "STAGING_QA", "AUTHORIZATION_CHECK", "PRODUCTION_DEPLOY",
    "PRODUCTION_SMOKE", "MONITORING", "deploy",
})
