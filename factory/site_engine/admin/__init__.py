"""Единая админка фабрики.

Одна панель на весь массив витрин. Она не содержит бизнес-логики: каждое
действие — вызов версионированного Control API v1 тем же путём, которым его
вызвала бы внешняя автоматика. Причина простая: логика, продублированная в
интерфейсе, расходится с API на первой же правке, и расходится молча — обе
стороны выглядят работающими.

Отсюда следствие, которое стоит назвать прямо: права оператора — это области
его токена, а не отдельная модель доступа админки. Кнопка, которую панель не
показывает, всё равно недоступна на уровне API; сокрытие — удобство, а не защита.
"""
from __future__ import annotations

ADMIN_COOKIE = "sfadmin"
SESSION_TTL_SECONDS = 8 * 60 * 60
SESSION_IDLE_SECONDS = 60 * 60
MAX_BODY_BYTES = 64 * 1024
CSRF_FIELD = "_csrf"

__all__ = [
    "ADMIN_COOKIE",
    "CSRF_FIELD",
    "MAX_BODY_BYTES",
    "SESSION_IDLE_SECONDS",
    "SESSION_TTL_SECONDS",
]
