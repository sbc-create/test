"""Совместимость: модуль переехал в домен редакции.

Перенос обязан не менять поведение, а смена пути импорта — это изменение для
того, кто импортирует. Прежнее имя оставлено работающим, пока потребители не
перейдут на `factory.site_engine.editorial.review_build`.

Обёртка не копия, а псевдоним: прежнее имя указывает на тот же объект модуля.
Разница не теоретическая. Первая обёртка реэкспортировала публичные имена, и
проверка, подменявшая приватную функцию через прежний путь, сломалась: копия
модуля отличается от модуля ровно тем, что подменять в ней нечего.

Обёртка временная. Убрать её можно будет, когда последний потребитель перейдёт
на новый путь, и не раньше: молчаливо сломать двенадцать мест ради опрятности
каталога — плохой обмен.
"""
from __future__ import annotations

import sys

from factory.site_engine.editorial import review_build as _модуль

sys.modules[__name__] = _модуль
