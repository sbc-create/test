"""Границы изменяемых настроек витрины — в одном месте для всех слоёв.

Список жил в модуле управляющего API, и читающему слою пришлось бы лезть в
его внутренности, чтобы показать оператору границы до ввода. Второй список,
написанный отдельно для экрана, разошёлся бы с проверкой на первом же
изменении и врал бы, оставаясь при этом похожим на документацию.
"""

from __future__ import annotations

import hashlib
from pathlib import Path
from typing import Any

# Обратимые настройки ядра: у каждой — проверка диапазона, а не только типа.
# Проверка типа пропускает keep_releases=0, после которого откатываться некуда.
SAFE_SETTINGS: dict[str, dict[str, Any]] = {
    "keep_releases": {"type": int, "min": 2, "max": 20},
    "cache_policy": {"type": dict, "value_type": int, "min": 0, "max": 86_400},
    "feature_flags": {"type": dict, "value_type": bool},
    # Признак публичной регистрации — настройка витрины, а не переменная среды.
    # Переменной он включается сразу везде и выключается перезапуском, то есть
    # на практике не выключается вовсе.
    "public_registration_enabled": {"type": bool},
    # Идентификаторы витрины: счётчик, партнёр, рекламная площадка.
    #
    # Это не секреты: по номеру счётчика ничего нельзя сделать, а без него не
    # понять, куда уходит статистика. Менять их безопасно и обратимо — выкладка
    # не нужна, потому что номер подставляется в разметку при отрисовке.
    #
    # Длина ограничена: идентификатор длиной в килобайт — это не идентификатор,
    # а попытка вписать в профиль что-то другое.
    "analytics_counter_id": {"type": str, "max_len": 64, "pattern": r"^[A-Za-z0-9_.:-]*$"},
    "advertising_partner_id": {"type": str, "max_len": 64, "pattern": r"^[A-Za-z0-9_.:-]*$"},
    # Какие источники содержимого витрине разрешены. Список закрытый: витрина
    # не вправе объявить источником что угодно, иначе разрешение перестаёт быть
    # разрешением.
    "content_sources_allowed": {"type": list, "value_type": str, "max_items": 8,
                                "pattern": r"^[a-z][a-z0-9-]{1,30}$"},
    # Как часто обновлять каталог. Нижняя граница не из осторожности: обход
    # каталога занимает около одиннадцати минут, и запуск чаще означает, что
    # следующий цикл начинается до конца предыдущего.
    "refresh_interval_minutes": {"type": int, "min": 10, "max": 1440},
}

# Отклоняются намеренно — с указанием причины в ответе, чтобы вызывающий понял,
# что это правило, а не пробел в реализации.
REFUSED_SETTINGS: dict[str, str] = {
    "domains": "смена доменов требует выкладки и проверки сертификатов",
    "canonical_host": "смена канонического хоста меняет индексацию всего сайта",
    "site_type": "тип сайта определяет адаптеры и хранилище",
    "indexing_enabled": "отключение индексации замечают через недели по падению трафика",
    "seo_enabled": "SEO-слой принадлежит другому потоку и меняется через него",
    "locale": "локаль меняет весь отрендеренный контент",
    "timezone": "часовой пояс меняет расписания и отметки времени",
    "theme": "оформление принадлежит потоку шаблонов",
    "render_mode": "режим рендеринга меняется вместе с выкладкой",
}


def profile_path(site_id: str, root: Path) -> Path:
    return root / "config" / "site-profiles" / f"{site_id}.json"


def config_version(path: Path) -> str:
    """Версия конфигурации — хэш её содержимого.

    Хэш, а не отметка времени: время меняется при копировании файла, содержимое —
    только при правке. Сверка по времени пропустила бы конкурентную запись.
    """
    if not path.exists():
        return "absent"
    return "sha256:" + hashlib.sha256(path.read_bytes()).hexdigest()[:32]
