"""Совместимость: модуль переехал в домен личности.

Псевдоним, а не копия: прежнее имя указывает на тот же объект модуля. Копия
отличается тем, что подменять в ней нечего, — это уже сломало проверку при
переносе домена редакции.

Обёртка временная: снять её можно, когда последний потребитель перейдёт на
`factory.site_engine.iam.operators`, и не раньше.
"""
from __future__ import annotations

import sys

from factory.site_engine.iam import operators as _модуль

sys.modules[__name__] = _модуль
