"""Совместимость: модуль переехал в домен заведения витрин.

Псевдоним, а не копия: прежнее имя указывает на тот же объект модуля. Копия
отличается от модуля тем, что подменять в ней нечего, — это уже сломало
проверку при переносе домена редакции, и повторять урок незачем.

Обёртка временная. Убрать её можно будет, когда последний потребитель перейдёт
на `factory.site_engine.provisioning.site_request`, и не раньше.
"""
from __future__ import annotations

import sys

from factory.site_engine.provisioning import site_request as _модуль

sys.modules[__name__] = _модуль
