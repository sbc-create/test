"""Домен редакции: правки поверх данных поставщика, разбор и публикация.

Первый домен, собранный физически, а не только объявленный. До этого граница
существовала записью в реестре, а файлы лежали вперемешку с чужими: сосед по
каталогу выглядит как часть целого, и через месяц им становится.

Публичный интерфейс — то, что реэкспортируется здесь. Всё остальное внутри
пакета устройство, и обращаться к нему снаружи нельзя: гейт границ проверяет
именно это.

Прежние имена модулей оставлены работающими тонкими обёртками
(`factory/site_engine/review_queue.py` и соседние). Потребителей двенадцать, и
ломать их переносом незачем: перенос обязан не менять поведение, а смена пути
импорта — это изменение для того, кто импортирует.
"""
from __future__ import annotations

from factory.site_engine.editorial import core as _core
from factory.site_engine.editorial.core import *  # noqa: F401,F403

#: Публичный интерфейс собирается из самого модуля, а не перечисляется руками:
#: второй список разошёлся бы с первым на первой же правке.
__all__ = [имя for имя in dir(_core) if not имя.startswith("_")]
