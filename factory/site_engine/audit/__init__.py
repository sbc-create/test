"""Audit/Action Ledger: неизменяемый журнал действий флота.

Хранилище, проверка личности, HTTP-обработчик, мост к реестру,
публикация ленты и резервное копирование. Данные журнала живут вне
репозитория: код версионируется, история системы — нет.

Здесь же — публичный интерфейс модуля `audit`: `AuditEvent`, `AuditLog` и
`event`. Они лежат в `events.py`, но потребитель берёт их отсюда и не обязан
знать, в каком файле они объявлены. Это не декоративная любезность: пока
`audit` был одним файлом, а стал пакетом, каждый импорт вида
`from factory.site_engine.audit import AuditLog` ломался, и обрыв сбора pytest
стоил не одного красного теста, а нуля выполненных. Реэкспорт делает
устройство пакета его внутренним делом.
"""
from factory.site_engine.audit.events import AuditEvent, AuditLog, event

__all__ = ["AuditEvent", "AuditLog", "event"]
