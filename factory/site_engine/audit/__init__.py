"""Audit/Action Ledger: неизменяемый журнал действий флота.

Хранилище, проверка личности, HTTP-обработчик, мост к реестру,
публикация ленты и резервное копирование. Данные журнала живут вне
репозитория: код версионируется, история системы — нет.
"""


# Прежний модуль редакторского журнала жил по адресу
# `factory/site_engine/audit.py` и был затенён этим пакетом. Имена
# ре-экспортируются, чтобы существующие импорты не переписывались: переезд
# файла — не повод менять чужой код.
from .editorial_log import *  # noqa: F401,F403
from . import editorial_log as _editorial_log

event = getattr(_editorial_log, "event", None)
