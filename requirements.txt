# Управляющий контур фабрики. Версии зафиксированы по фактически проверенным в среде.
jsonschema==4.26.0
PyYAML==6.0.1
Jinja2==3.1.6
# dev / QA
pytest==9.1.1

# --- добавлено SEO-оператором (пины фабрики не понижаются) ---
rfc3339-validator==0.1.4
ruff==0.6.9

# --- Secret Hub ---
# AES-256-GCM для значений в хранилище и эфемерный TLS-сертификат одноразовой
# формы. Собственная реализация блочного шифра была бы худшим из возможных
# решений, а хранить credentials в открытом виде запрещено.
cryptography==46.0.3
