[Service]
# Две точки отката вместо четырёх.
#
# Полный каталог даёт релиз около 1,6 ГБ вместо прежних 120 МБ, и четыре
# хранимые копии на трёх сайтах на диск не помещаются. Две — это текущий релиз
# и один предыдущий: откатиться есть куда, а места хватает.
Environment=LORDS_KEEP_RELEASES=2
