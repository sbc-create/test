# Живой смоук Animedia после root-перезапуска — 2026-09-21

Владелец выполнил перезапуск `nova-animedia-01.service` под root: PID 4178279,
оба домена HTTP 200. Ниже — что этот перезапуск фактически поднял и что
проверено на живых доменах.

## Что поднял перезапуск

Не кандидата B16. При разборе аварии 2026-09-21 назначение и подпись обеих
витрин были переведены на цель откáта, зарядка `7aae3d2c…` снята — поэтому
процесс стартовал в цель откáта:

| | |
| --- | --- |
| release | `20260921T154512Z-8966632-animedia-rollback` |
| исходный коммит релиза | `89666321093651d4bd5b06a00645fdf903124810` |
| artifact sha256 | `d2e9628f2a4b14c56ef28a6814b53f49cdb4017db1ab8ddebebb79aaad39e908` |
| build_id | `20260920T102102Z-89666321-nova` |
| animedia-01 (порт 9121) | PID **4178279**, старт 2026-09-21 16:30:08 UTC |
| animedia-02 (порт 9122) | PID 2693863, старт 2026-09-20 13:20:16 UTC |
| кандидат `7aae3d2c…` живой | нет |

Обе витрины отдают одну и ту же личность, и она совпадает с назначенным
релизом: расхождения кода и подписи нет. Процесс animedia-02 старше загрузчика
и исполняет код из памяти, но байт в байт тот же `d2e9628f…`, поэтому его
будущий перезапуск личность не сдвинет.

## Что проверено

Четыре прогона `automation/host/animedia-b16-smoke.py --mode incident`: по два
на каждый домен, 9 маршрутов × 390/768/1440 = 27 ячеек на прогон, 108
скриншотов. Ожидаемая личность задана по фактически отдаваемой, а не по пакету
B16.

| прогон | домен | AVAILABILITY_PASS | SMOKE_PASS | провалов |
| --- | --- | --- | --- | --- |
| 1 | animedia.space | True | True | 0 |
| 1 | animedia.icu | True | True | 0 |
| 2 | animedia.space | True | True | 0 |
| 2 | animedia.icu | True | True | 0 |

Во всех 108 ячейках: коды маршрутов как ожидалось (7 × 200), отсутствующий
маршрут и `?page=99999` — **404**, `meta robots` и `X-Robots-Tag` —
`noindex, nofollow`, canonical строго по своему домену, горизонтальное
переполнение 0 px, обрезанного обязательного текста нет, плеер один, `h1` один,
второй домен не сдвинулся.

## Наблюдение, не провал

Знак сборки в подвале присутствует (27 ячеек в каждом прогоне) — это известный
дефект B14 сборки `20260920T102102Z-89666321-nova`, который закрывает только
кандидат. В аварийном режиме он печатается наблюдением; гейт приёмки
`--mode deploy` на нём по-прежнему падает и не ослаблен.

## Мутации

Откат не потребовался — провалов нет. Выкат, перезапуск, DNS и индексация со
стороны сессии не выполнялись; Zona не затрагивалась.
