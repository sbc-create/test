# Кадры staged-проверки B16

Выборка: коллекции — новая поверхность блока B13, и страница тайтла с плеером.
Все три требуемые ширины: 390, 768, 1440.

Снято с релиза `20260921T153817Z-c8c4587-animedia-b16` (артефакт `7aae3d2c…`)
на боевом снимке каталога, домен разрешён в петлю — наружу запросов нет.

Полный набор из 24 кадров (все восемь маршрутов × три ширины, 12 МБ) в историю
не идёт: измерения лежат в `STAGED_CHECK.json`, а кадры пересоздаются командой

    .venv/bin/python <стенд staged-проверки> 

Числа важнее картинок: по кадру не видно ни переполнения в один пиксель, ни
того, какую сборку витрина объявила.
