# Root causes (BLOCK_01)

1. **IA mismatch**: reference header encodes taxonomy + search density; ours is a single compact strip (`CSS_GEOMETRY_BUG` / later BLOCK_02).
2. **Title/episode composition**: problem title and episode routes render core data but lack reference-like secondary structure and episode context density (`TEMPLATE_RENDER_BUG` → BLOCK_08/10).
3. **Unsupported social modules**: diaries/community on reference must not be copied (`OWNER_DATA_GAP` / product scope).
4. **Source gaps**: empty description for some titles; AMD ratings absent from snapshot — honest empty, not invention.
