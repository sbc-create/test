# TEMPLATES-ZONA-ANIMEDIA-OWNER-REVIEW-007 — свидетельства

`FINAL_STATUS = BLOCKED` — по одной причине: **доставка пакета владельцу в сессию
закрыта профилем разрешений репозитория**. Сам пакет собран целиком.

## Блокер

Задание требует положить материалы «непосредственно в Artifacts этой Claude-сессии».
Оба механизма доставки отклонены guard'ом:

```
Artifact      → неизвестный инструмент 'Artifact': правило не описано, действует default-deny
SendUserFile  → неизвестный инструмент 'SendUserFile': правило не описано, действует default-deny
```

Обходить guard запрещено заданием и правилами репозитория, поэтому вызовы не
повторялись. Требуется одно действие владельца — разрешить инструмент `Artifact`
(и/или `SendUserFile`) в настройках разрешений проекта. После этого публикация
выполняется без единой правки пакета: он лежит готовым.

Пока доставка закрыта, пакет доступен из отправленной ветки на GitHub.

## Что собрано

| файл | содержимое |
| --- | --- |
| `owner-package/OWNER-VISUAL-REVIEW-standalone.html` | **самодостаточная страница, 1.89 МБ**: все изображения встроены, открывается двойным щелчком |
| `owner-package/OWNER-VISUAL-REVIEW.html` | та же страница со внешними файлами (для публикации артефактом) |
| `owner-package/pairs/` | 51 композит «было \| стало»: 8 архетипов Zona и 9 Animedia × 3 размера |
| `owner-package/zona-contact-sheet.png`, `animedia-contact-sheet.png` | по одному листу на витрину |
| `owner-package/manifest.json` | маршруты, размеры, SHA-256 кадров, измерения эталона и кандидата |
| `reference-capture.json`, `reference-shots/` | 36 кадров эталонов: Zona 4 маршрута, Animedia 8 |
| `candidate-capture.json`, `shots/` | 105 кадров: кандидаты, прежние версии, боевые данные Animedia |
| `missing-reference-routes.json` | 6 маршрутов эталона, которых получить не удалось, с точной причиной |
| `browser-matrix.json`, `accessibility-and-browsers.json` | Chromium, Firefox, WebKit + клавиатура и семантика |

## Охват эталонов

Было 1/1 на витрину. Стало: **Zona 4 маршрута**, **Animedia 8**. Маршруты внесены
в `inventory/reference-sources.yaml` поимённо, без wildcard, только GET, без
cookies, авторизации и отправки форм.

Не получено и честно записано: `/updates/` Zona отдаёт 404 на самом эталоне;
поиск, пустой результат и результаты фильтра достижимы только отправкой формы,
что задание прямо запрещает; прямых ссылок на страницу тайтла в каталоге
эталона Animedia не обнаружено. Ни одна из этих страниц не заменена снимком
главной и не названа проверенной.

## Кадры самих эталонов наружу не публикуются

Это страницы третьих лиц с их постерами, названиями и брендингом. Они сняты и
хранятся локально по санкционированной политике измерения
(`inventory/reference-sources.yaml`, `store.screenshots: true`), но в пакет,
уходящий наружу, не включены. Вместо них приведены **измеренные числа и цвета** —
именно по ним и судится паритет, и они даны полностью.

## Браузеры и доступность

| | Chromium | Firefox | WebKit |
| --- | --- | --- | --- |
| страниц | 12 | 12 | 12 |
| переполнение | 0 | 0 | 0 |
| ошибки консоли | только на `/nope/` | 0 | только на `/nope/` |
| h1, alt, подписи полей, ориентиры, lang | есть | есть | есть |
| клавиатура: шагов с видимым фокусом | 12/12 | 12/12 | 12/12 |
| пропуск к содержимому первым | да | да | да |

Ошибки консоли на `/nope/` — сам ответ 404, то есть требуемая настоящая 404.
Полный аудит axe-core не запускался.
