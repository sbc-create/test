# Откат

## Сейчас откатывать нечего

Production в этой задаче не изменялся. Версии на момент сдачи:

| витрина | версия | чем подтверждено |
| --- | --- | --- |
| `lordfilm47.space` | 1.1.0 | манифест не трогался; байты ответа совпадают побайтово (`lords-unchanged-proof.json`) |
| `zonafilm.space` | 1.1.0 | выкладки не было |
| `animedia.icu`, `animedia.space` | 1.0.2 | выкладки не было |

Ни установка, ни canary, ни откат, ни restore-forward, ни перезапуск юнитов не
выполнялись. `sudo` и `systemctl` не запускались.

## Если кандидат будет выложен после приёмки

Откат — возврат ОДНОГО поля манифеста витрины: `design_version` с `1.2.0`
обратно на прежнее значение. Это свойство кода, а не соглашение: оформление
включается набором `ОФОРМЛЕНИЕ_ВЕРСИИ`, и витрина, чей манифест остался на
прежней версии, исполняет прежние ветки и отдаёт прежние байты.

Штатная точка отката снимается установщиком `automation/host/lords-nova-canary.py`
до записи; он же возвращает витрину сам, если публичные ворота не прошли.

Откат обязателен при: 5xx, битых ссылках, чужой сущности, горизонтальном
переполнении, несовпадении SHA и визуальной регрессии.
