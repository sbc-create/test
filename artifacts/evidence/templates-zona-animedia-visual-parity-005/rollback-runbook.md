# Откат

Откатывать нечего: production в этой задаче не изменялся.

| витрина | версия до задачи | версия после задачи |
| --- | --- | --- |
| `lordfilm47.space` (lords-01) | 1.1.0 | 1.1.0 |
| `zonafilm.space` (zona-01) | 1.1.0 | 1.1.0 |
| `animedia.icu` (animedia-01) | 1.0.2 | 1.0.2 |
| `animedia.space` (animedia-02) | 1.0.2 | 1.0.2 |

Ни установка, ни откат, ни restore-forward, ни перезапуск юнитов не выполнялись.
`sudo` и `systemctl` в этой задаче не запускались.

Если владелец в будущем выложит кандидата, точкой отката служит штатный механизм
установщика `automation/host/lords-nova-canary.py`: он снимает точку до записи и
возвращает витрину сам при провале публичных ворот.
