# Что осталось и почему это вне границы сессии

## P0 — zonafilm.space: исправление установлено, активация невозможна

Релиз `zona-01-fca1d54eeeb7` установлен на обе дороги активации, пред-активация
на полном боевом снимке (53 573 записи) — PASS, установленный артефакт с
боевым манифестом даёт `ОФОРМЛЕНИЕ_НОВОЕ=True`. Не хватает одного: перезапуска
службы. Перебраны все пути:

| Путь | Результат |
| --- | --- |
| `systemctl restart nova-zona-01.service` | `Interactive authentication required` |
| `sudo -n systemctl restart …` (форма из скриптов репозитория) | `[factory-guard G-PRIV] Неконтролируемый sudo/su запрещён` |
| polkit-правила | каталогов правил нет |
| `site-factory-worker.service` | `inactive` |
| control API `127.0.0.1:8790` | маршрутов deploy/restart нет: только реестр, релизы, задания |
| `inventory/ssh-hosts.yaml` → `sudo_allowlist` | `hosts: []` — слой выкладки не настроен, разрешать нечего |

Процесс витрины принадлежит пользователю `lords`, сессия — `claude`, сигнал
ядром отклоняется. Санкционированного способа перезапуска в этом окружении
не существует.

Одна команда владельца: `sudo systemctl restart nova-zona-01.service`
(без отдельного `daemon-reload` — обе дороги уже ведут на один релиз).

## P1 — yummyani.biz / .org / .site: дефект внутри чужого контейнера

Ссылка `/anime/vladyka-mnozhestva-mirov--019152a4-5711-7b7e-bc08-a27c6962bd32`
отдаёт 404, короткий адрес `/anime/vladyka-mnozhestva-mirov` — 200. Обе
стороны — и генератор ссылки, и резолвер маршрута — живут в контейнере
`yummyani/web:staging` (127.0.0.1:3101 → 172.19.0.3:3000). Контейнер был
пересобран за 11 минут до проверки: лента Yummy сейчас в работе другой сессии.
Правка из Zona-worktree нарушила бы изоляция лент, поэтому не делалась.

Причина названа точно и воспроизводится одной парой запросов — этого
достаточно, чтобы лента Yummy закрыла дефект у себя.

## P1 — zonafilm.cc: лента занята активной чужой сессией

`nova-zona-02.service` — `inactive`; порт 9123 держит процесс другой сессии
(`uid=claude`, релиз `zona-02-1015c650d8be`, код `7ccf094a` — сборка ДО
настоящего слайдера). Нет `zona-02-popular-weekly.json` и `player-zona-02.json`,
поэтому недельный блок и кнопка «Смотреть» отсутствуют. Блока nginx для
`zonafilm.cc` среди читаемых конфигов нет.

Маршруты при этом целы: 60 из 60 адресов отвечают, битых внутренних ссылок
ноль. Вмешательство в витрину, которую прямо сейчас собирает другая сессия,
дало бы третье, никем не проверенное состояние, поэтому не делалось.
