# SORTING_CONTRACT

```text
published_at DESC
source_episode_id DESC
event_id DESC
```

Field `published_at` = catalog item publish timestamp from the snapshot.
Not title year, not deploy time, not page-build time.
Identical snapshot → identical order digest.
