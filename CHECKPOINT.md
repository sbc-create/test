# CHECKPOINT — PORTABLE-ANIMEDIA-SPACE-01

Живой журнал задачи. Обновляется после каждого этапа. Строка «не выполнено»
здесь означает, что проверка не запускалась, а не что она прошла.

## Цель

Выделить **только** animedia.space в самостоятельный приватный Git-репозиторий и
отдельное развёртывание. Следующий сайт не начинать.

## Подтверждённая цепочка (этап 1, измерено)

| Звено | Значение | Чем подтверждено |
| --- | --- | --- |
| Домен | `animedia.space` | `/etc/nginx/lords/animedia-02.conf` |
| tenant | `animedia-02` | тот же конфиг, комментарий и unit |
| nginx → upstream | `127.0.0.1:9122` | `proxy_pass` в обоих server-блоках |
| Служба | `nova-animedia-02.service` | `--port 9122` в `ExecStart` |
| Пользователь службы | `lords` | `User=lords` |
| Релиз | `/srv/lords/animedia-02/current` → `releases/d45af5669e3d` | симлинк |
| Предыдущий релиз | `releases/852aae298457` | симлинк `previous` |
| Данные | `/srv/lords/animedia-02/data/animedia-community.json` | режим 600, владелец `lords` |
| Каталог | `/srv/lords/.frontend/animedia-02-catalog.json` | 7426 записей, 2.5 МБ |
| Шаблон | `/srv/lords/.frontend/template-manifest-animedia-02.json` | путь из unit |
| PUB | `10252` | `/srv/lords/.frontend/player-animedia-02.json` |
| Страниц | 1395 `index.html`, 1514 файлов, 97 МБ | `find` по `current/site` |

`animedia.icu` — отдельный сайт (`animedia-01`, unit `nova-animedia-01.service`).
В этой задаче не трогается.

**Общая изменяемая зависимость, которую требуется убрать:** служба работает из
`/srv/lords/.frontend` — общий каталог всех витрин направления. Оттуда берутся и
исполняемый файл `lords-frontend.py`, и каталог, и манифест шаблона.

## Состояние этапов

| Этап | Статус | Результат |
| --- | --- | --- |
| 1. Сохранить работу | ВЫПОЛНЕН | push `311467e46deac7f10aaf660628f2a2dda5ed6502`, удалённый SHA сверен и совпадает |
| 2. Выделить сайт | ВЫПОЛНЕН | приватный `sbc-create/site-animedia-space`, ветка `claude/extract-animedia-space`, SHA `a0b89677c69f9d33a6502c2c758d417eb03c11b2` (локальный = удалённый) |
| 3. Данные и обновления | ВЫПОЛНЕН на копии | резервная копия + восстановление PASS; доставка обновления каталога PASS; комментарии и голоса пишутся в собственное хранилище |
| 4. Проверить и выложить | ЧАСТИЧНО | проверки на изолированной копии пройдены; **живое переключение не выполнялось** — нет sudo/systemctl |

## Результаты этапа 2–4 (измерено)

| Что | Итог |
| --- | --- |
| Репозиторий | `https://github.com/sbc-create/site-animedia-space` (private=true) |
| Ветка / SHA | `claude/extract-animedia-space` / `a0b89677c69f9d33a6502c2c758d417eb03c11b2` |
| Digest релиза | `sha256:1017b15ec0a24d1d105b875964833c0ae4226d01a95caaa1945199697e36263d` (две сборки дали один digest) |
| Происхождение | sha256 всех пяти модулей `src/` совпадает с исполняемым релизом `20260922T143111Z-efdef56-animedia-parity` |
| Закреплённый коммит | `efdef56e5fd19cda8ed3a449306c833de2e5c77d` |
| Общая зависимость | убрана: у изолированного процесса **0** путей под `/srv/lords/.frontend` |
| Резервная копия | `var/backups/animedia-space/20260922T213506Z`, 7 файлов; восстановление сверено по sha256 |
| Изолированный запуск | из дерева, извлечённого из коммита: 7426 тайтлов, 7426 подробностей, 56 жанров, плеер подключён |

## Найденная ловушка (главное техническое основание задачи)

Все умолчания рантайма указывают на соседний сайт `animedia-01`: каталог,
манифест шаблона, старый корень и запасной идентификатор витрины. Одна
незаданная переменная — и animedia.space отдал бы каталог соседа и писал бы
комментарии в его хранилище. Поэтому запуск идёт через `run.py`, который
выставляет всё явно и отказывается стартовать при пути к соседу.

## Ресурсы и доступы (этап 1, измерено)

- Диск: свободно 58.4 ГБ из 203.3 ГБ.
- RAM: доступно 24.6 ГБ из 29.4 ГБ. Загрузка 1.78 / 2.15 / 2.24.
- Конкурирующей выкладки нет: в `var/locks/` только собственные снятые
  ledger-замки, блокировок `site+environment` для `animedia-02` нет.
- GitHub: `gh` 2.98.0, учётная запись `sbc-create`, scopes `gist, read:org,
  repo, workflow` — приватный репозиторий создать можно.

## Внешние ограничения, обнаруженные на этапе 1

| Ограничение | Следствие |
| --- | --- |
| `sudo` и `systemctl` запрещены `deny`-списком `.claude/settings.json` | Живое переключение на текущем хосте невозможно: нельзя ни создать unit, ни перечитать nginx. Полномочие владельца это не снимает — `deny` сильнее любого разрешения. |
| `animedia-community.json` имеет режим 600 и владельца `lords` | Комментарии и голоса не читаются без `sudo`: резервная копия и сверка строк по ним невозможны. |
| `animedia.space` отсутствует в `inventory/network-allowlist.yaml` | Проверка публичного домена снаружи — `BLOCKED_INPUT`, расширять список по своей инициативе запрещено. |

## Команда продолжения

```bash
cd /home/claude/wt-portable-site-cell-01 && git status --short && cat CHECKPOINT.md
```

## Результаты проверок (этап 4, измерено)

Сравнение: живой экземпляр `127.0.0.1:9122` против собранного из чистого
коммита `127.0.0.1:9423`, данные — восстановленная копия.

| Ворота | Итог | Чем подтверждено |
| --- | --- | --- |
| URL_INVENTORY | PASS | 200 адресов произведений: расхождений 0, 404 в чистой сборке 0 |
| ROUTES_SAMPLE | PASS | 46 маршрутов релиза: коды совпали 46/46, включая 404 |
| PAGES | PASS | главная, каталог, поиск, фильтр, неизвестный адрес — коды совпали |
| UNKNOWN_404 | PASS | несуществующий адрес отдаёт 404 на обоих |
| PLAYER / PUB | PASS | `video-player` в разметке, PUB=10252, отозванных 10331/10332/10333 нет |
| IMAGES / RATINGS | PASS | `<img>` и блок оценок присутствуют |
| COMMENTS | PASS | создание, чтение, сохранение после перезапуска |
| VOTING | PASS | голос 1–10 принят, сводка 9.0 из 1 голоса, пережил перезапуск |
| RESPONSIVE | PASS | `name="viewport"` и адаптивные правила в разметке |
| BACKUP / RESTORE | PASS | 7 файлов, восстановление сверено по sha256, расхождений 0 |
| CONTENT_SYNC | PASS | новая запись доехала за 20 с; повтор снимка дубля не создал; удаление доехало |
| SHARED_DEPENDENCY_REMOVED | PASS | у процесса 0 путей под `/srv/lords/.frontend` (открытые файлы + карта памяти) |
| RELEASE_DIGEST | PASS | две сборки одного коммита дали один digest |
| NEIGHBOURS | PASS | шесть живых витрин отвечают 200; файлы данных не изменялись |
| VISUAL_390_1440 | NOT_RUN | проверена разметка (viewport, media-queries), не рендеринг в браузере |
| LIVE_CUTOVER | NOT_RUN | нет sudo/systemctl |
| CROSS_HOST_TEST | NOT_RUN | целевой сервер не предоставлен |
| METRIKA | NOT_CONFIGURED | счётчика у animedia.space нет и не было |
| TOPVISOR | NOT_CONFIGURED | проекта у animedia.space нет и не было |

## Найденные чужие дефекты (не исправлялись здесь)

1. **Профиль `config/site-profiles/animedia-02.json` несёт чужие значения:**
   `analytics.counter_id = 112010269` и `allowed_hosts = [lordfilm47.space]`,
   `seo.canonical_host = lordfilm47.space`, `seo.editorial_profile =
   lords-01-editorial` — всё это принадлежит сайту `lords-01`.
   Сейчас дефект **латентный**: в живую выдачу ни счётчик, ни `lordfilm47` не
   попадают (Метрика не подключена, canonical выводится из Host). Но включение
   аналитики или SEO по этому профилю отправило бы данные animedia.space в
   чужой счётчик и объявило бы чужой canonical.
2. **TLS-сертификат animedia.space выписан на `zonafilm.space`**
   (`/etc/nginx/lords/animedia-02.conf`, `ssl_certificate .../zonafilm.space/`).
   Для HTTPS на собственном домене это несоответствие имени.

Оба — вне границ этой задачи (правка профилей и сертификатов соседей). Записаны,
чтобы не выдать их за проверенные.

## Что нужно для живого переключения

1. `sudo`/`systemctl` — сейчас запрещены `deny`-списком `.claude/settings.json`;
   полномочие владельца этого не снимает.
2. Учётная запись `animedia-space` и каталоги `/srv/animedia-space/{app,data}`.
3. Копия `animedia-community.json` (режим 600, владелец `lords`) — нужна для
   переноса комментариев и голосов.
4. `animedia.space` в `inventory/network-allowlist.yaml` — иначе публичный
   домен снаружи не проверяется.

## Команда продолжения

```bash
cd /home/claude/wt-portable-site-cell-01 && cat CHECKPOINT.md
# живое переключение (нужен root):
sudo /home/claude/wt-portable-site-cell-01/var/site-repos/animedia-space/deploy/install.sh
# откат:
sudo /home/claude/wt-portable-site-cell-01/var/site-repos/animedia-space/deploy/rollback.sh
```

---

# Продолжение: PORTABLE-ANIMEDIA-SPACE-01, второй заход

## Исправленное по существу

| Что | Было | Стало |
| --- | --- | --- |
| Профиль `animedia-02` | счётчик, allowed_hosts, canonical_host и editorial_profile сайта **lords-01** | `counter_id: null`, сбор выключен, `animedia.space` и `animedia-02-editorial`; номер чужого счётчика не повторяется даже в пояснении |
| Экспортированный профиль | тех же полей не было вовсе | `canonical_host`, `editorial_profile`, `analytics`, `topvisor` записаны со своими значениями |
| deny-правила | `/permissions` убрала 4 защитных правила из рабочей копии | восстановлены; относительно HEAD список deny снова полный (74) |
| CI | workflow лежал файлом, `gh run list` пуст — не запускался ни разу | триггер на рабочую ветку; три прогона, все успешны |
| digest | CI и локально давали **разные** digest одного коммита | режим файлов канонизирован; digest совпадает на двух машинах |

## Доказательства

| Проверка | Итог | Чем подтверждено |
| --- | --- | --- |
| TLS по SNI и SAN | PASS | SAN сертификата: `animedia.icu`, `animedia.space`, `zonafilm.space`. CN — `zonafilm.space`, но домен **покрыт**. Прежний вывод «сертификат не тот» был сделан по одному CN и был неверен |
| Браузер 390 px | PASS | главная, каталог, произведение: код 200, горизонтального переполнения 0 px, ошибок консоли нет |
| Браузер 1440 px | PASS | то же; плеер присутствует на странице произведения |
| CI/CD работает | PASS | прогоны 35791498455, 35791655346, 35792156061, 35792462379 — все success, 10/10 проверок на раннере |
| Воспроизводимость сборки | PASS | коммиты `a080fd0`, `00faace`, `d737117`: CI и локальная сборка дают один digest. Итоговый — `sha256:70595a3020bb…` |
| Обновление каталога | PASS | изменение доехало за ~35 с и применилось |
| Локальные записи при обновлении | PASS | комментарий и голос (1) сохранились после обновления каталога и оценок |
| Отдельный репозиторий обязателен | PASS | 14 тестов: повтор не создаёт второй репозиторий, сорвавшееся создание блокирует выпуск, монорепозиторий как remote отвергается |
| Метрика | NOT_RUN | своего счётчика нет; запись заведена с `counter_id: null`. Штатная проверка сама даёт `BLOCKED_DEPLOYMENT` |
| Topvisor | BLOCKED | нет доступа к каталогу учётных данных (группа-владелец) |
| Активация | NOT_RUN | `sudo` и `systemctl` в `deny` |
| Публичный домен снаружи | NOT_RUN | `animedia.space` нет в `inventory/network-allowlist.yaml` |
| Настоящий запуск плеера | NOT_RUN | плейлист провайдера — `plapi.cdnvideohub.com`, в allowlist только `public-api.cdnvideohub.com` |
| CROSS_HOST_TEST | NOT_RUN | второй сервер не предоставлен |

## Одна команда для активации

```bash
sudo /home/claude/wt-portable-site-cell-01/var/site-repos/animedia-space/deploy/activate.sh --dry-run
sudo /home/claude/wt-portable-site-cell-01/var/site-repos/animedia-space/deploy/activate.sh
```

Откат:

```bash
sudo /home/claude/wt-portable-site-cell-01/var/site-repos/animedia-space/deploy/rollback.sh
```

Скрипт останавливает старую службу **до** копирования комментариев (два
экземпляра не пишут одновременно), и сам возвращает прежнюю службу, если
`healthz` не ответил за 40 с.

## Следующие сайты

`zonafilm.space` и остальные **не начаты**: задание ставит их после живой
приёмки animedia.space, а она не выполнялась. Процесс для них уже закреплён
кодом — отдельный репозиторий обязателен, повтор не плодит дубли.

## Итоговое состояние animedia.space

| Поле | Значение |
| --- | --- |
| Репозиторий | `https://github.com/sbc-create/site-animedia-space` (private) |
| Ветка | `claude/extract-animedia-space` |
| SHA (локальный = удалённый) | `d73711705ebc9cb25ab2419abd8a6db9fb1ee42f` |
| Digest релиза | `sha256:70595a3020bb99dfcd74b889e368eab91b14b89564bd3744755d1e42e35f7097` |
| Собран | CI 35792462379 и локально — digest совпал |
| Действующий runtime | `nova-animedia-02.service`, pid 951, порт 9122, релиз `d45af5669e3d` — **не тронут** |
| Статус | `staged`. **Не DEPLOYED**: переключения не было |

## Замечание о параллельном терминале

Во время работы соседний терминал комментариев и оценок перезапустил
lords-01/02/03 — витрины на полминуты не отвечали. К animedia-02 это отношения
не имеет (её процесс не перезапускался с 20:32 UTC), но показывает, почему
активация теперь читает общий замок выкладки и отказывается работать поверх
чужой.
