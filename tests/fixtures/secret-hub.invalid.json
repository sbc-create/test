{
  "version": 1,
  "note": "Фикстура отказа. Нарушений три, и каждое — то, ради чего схема написана: (1) file_mode 0644 открывает секрет группе и миру; (2) method POST превращает read-only проверку в запись; (3) у systemd-потребителя нет ни unit, ни dropin, ни credential_names — применять нечего, а угадывать имя unit'а фабрика не станет. Схема обязана отвергнуть документ, а не подобрать недостающее.",
  "store_dir": "/var/lib/example-secret-hub",
  "socket_path": "/run/example-secret-hub/hub.sock",
  "provider": {
    "name": "cdnvideohub",
    "verify": {
      "base_url": "https://public-api.cdnvideohub.com/api/v1/",
      "path": "countries",
      "method": "POST",
      "auth_header": "Authorization",
      "auth_scheme": "Bearer",
      "provenance": "фикстура отказа: проверка обязана быть read-only"
    }
  },
  "portfolios": [
    {
      "id": "example-broken",
      "title": "Направление с открытыми правами",
      "enabled": true,
      "consumers": [
        {
          "id": "example-broken-01",
          "kind": "systemd_credential",
          "title": "потребитель без unit",
          "directory": "/etc/example/secrets/broken",
          "files": {
            "api_token": "api-token",
            "publisher_id": "publisher-id"
          },
          "owner": "root",
          "group": "root",
          "file_mode": "0644",
          "directory_mode": "0700",
          "reload": {
            "kind": "none"
          }
        }
      ]
    }
  ]
}
